apiVersion: 1

contactPoints:
  - orgId: 1
    name: telegram-alerts
    receivers:
      - uid: telegram_receiver
        type: telegram
        settings:
          bottoken: ${TELEGRAM_BOT_TOKEN}
          # Hardcoded: Grafana re-parses env-substituted negative integers as YAML numbers,
          # breaking JSON unmarshal into string. Update here when chat_id changes (also update .env GRAFANA_ALERT_CHAT_ID).
          chatid: "-5193354760"
