Based on the sources and the architecture of the stack you are building (CopilotKit, A2UI, LangGraph, Keycloak, and MCP), here is a comprehensive list of frontend functions and capabilities, categorized by user-facing SDK hooks, UI components, and administrative/management features. \#\#\# **1\. Standard Core SDK Functions (Hooks)** These are the primary React hooks used to build the agentic experience for any user 1-3. \* **useFrontendTool (v2) / useCopilotAction (v1)**: Registers client-side functions that the AI agent can call 4, 5\. This allows the agent to take real-time actions in the browser, such as triggering animations, modifying local state, or accessing browser APIs like localStorage 5, 6\. \* **useAgentContext (v2) / useCopilotReadable (v1)**: Provides the AI agent with "eyes" to see the application's current state 7-9. It serializes frontend data (e.g., a list of tasks or the current canvas structure) and injects it into the agent's context 10, 11\. \* **useCoAgent**: Manages the connection and bidirectional state synchronization with a backend "CoAgent" (like a LangGraph agent) 2, 12\. It allows the frontend to subscribe to the agent's internal status in real-time 12, 13\. \* **useCoAgentStateRender**: Specifically used to render different UI components based on the agent's current node or execution status (e.g., showing a progress bar while the agent is "fetching data") 2, 12\. \* **useHumanInTheLoop / renderAndWait**: Implements the Human-in-the-Loop (HITL) pattern by pausing agent execution to request user confirmation or edits through a custom component 14-16. \* **useCopilotChat**: A headless hook that provides raw chat state (messages, input text, loading status) for developers building entirely custom chat interfaces from scratch 2, 17\. \* **useRenderToolCall**: Detects when a backend tool is called and triggers a specific frontend component to render the progress or results 18, 19\. \* **useSuggestions / useConfigureSuggestions**: Generates and manages contextual autocomplete or command suggestions based on the current app state 9, 20\. \#\#\# **2\. Built-in UI Components** Ready-made components that provide a polished user experience out of the box 17, 21\. \* **\*\*: A standard streaming chat interface that supports message history, tool call rendering, and agent responses \[21-23\]. \* \*\***: A floating, collapsible chat bubble interface 17, 24\. \* **\*\*: An integrated sidebar chat panel \[17, 24\]. \* \*\***: A critical component for this stack that listens to A2UI-formatted JSON streams from the agent and renders dynamic widgets (charts, forms, interactive tables) in real-time 25, 26\. \* **\`\`**: An AI-powered replacement for standard text areas that provides context-aware autocomplete and editing 3, 20\. \#\#\# **3\. User-Level Functional Capabilities** These represent the actions a standard user performs through the frontend interaction loop 27, 28\. \* **Real-time Streaming Interaction**: Users receive token-by-token text responses and live progress updates on tool execution 29, 30\. \* **Collaborative Canvas/Shared State**: Users and agents can co-work on shared data (like a spreadsheet or research canvas), where changes made by one are immediately reflected for the other 31-33. \* **Generative UI Interaction**: Users interact with ephemeral UI components generated "on the fly" by the agent, such as approving a specific travel itinerary or filling out an incident report form guided by the agent 34-37. \* **Disambiguation & HITL**: Users resolve ambiguities when the agent pauses at a breakpoint to ask for clarification or approval of a proposed plan 38, 39\. \#\#\# **4\. Administrator & Management Functions** These functions are necessary for the complex enterprise stack you described, including RBAC, ACL, and workflow management. \* **ACL Management UI**: Based on the stack's requirements, you can build a **Management Canvas** using **A2UI** to render a dynamic "Permission Matrix" proposed solution. Administrators can use this to toggle access to specific tools, sub-agents, or MCP functions for different user groups 40, 41\. \* **Dynamic Tool Availability**: Admins (or the system logic) use the **available** property on useFrontendTool to programmatically enable or disable tools based on the user's Keycloak roles 40, 41\. A "disabled" tool is completely hidden from the agent's reasoning context 5\. \* **Low-Code Workflow Studio**: A drag-and-drop interface (using the **Canvas Starter** pattern) where admins connect nodes representing Agents, Skills, and MCP tools to define a LangGraph StateGraph 39, 42, 43\. \* **Job Scheduler Dashboard**: A frontend interface to define, schedule, and monitor the status of background jobs (e.g., "Run a web search every Monday") proposed solution. \* **Agent Inspector**: A specialized debugging tool accessible via the UI that allows administrators to inspect agent runs, view state snapshots, and trace tool calls as they stream from the backend 44, 45\. \* **Security Context Filtering**: Admins use the **categories** parameter in useCopilotReadable to restrict which parts of the application state are visible to the agent based on the user's sensitivity level 46\. \#\#\# **Summary Table: Frontend Functions** | Category | Primary Functions / Components | | :--- | :--- | | **Logic Hooks** | useFrontendTool, useCopilotReadable, useCoAgent, useHumanInTheLoop 2, 3, 9 | | **UI Components** | CopilotChat, CopilotSidebar, A2UIMessageRenderer, CopilotTextarea 17, 20, 25 | | **User Capabilities** | Real-time chat, HITL Approvals, Shared Canvas editing, Generative UI interactions 15, 32, 37, 47 | | **Admin Functions** | ACL Management (A2UI), Dynamic Tool Toggling (available), Agent Inspector, Low-code Workflow definition 405, 591, 809, proposed solution |  
