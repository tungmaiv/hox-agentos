---
phase: 07-hardening-and-sandboxing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/security/test_isolation.py
autonomous: true
gap_closure: true
requirements:
  - SBOX-01
  - SBOX-02
  - SBOX-03

must_haves:
  truths:
    - "test_user_a_cannot_read_user_b_conversation_turns passes when run as a standalone file (pytest tests/security/test_isolation.py in isolation, not as part of full suite)"
    - "All 5 isolation pen tests pass regardless of test run order (4 pass + 1 skipped for pgvector)"
    - "No implicit dependency on test collection side-effects for ORM model registration"
  artifacts:
    - path: "backend/tests/security/test_isolation.py"
      provides: "Cross-user isolation pen tests — all deterministic regardless of run order"
      contains: "import core.models"
  key_links:
    - from: "backend/tests/security/test_isolation.py"
      to: "backend/core/models/__init__.py"
      via: "import core.models at module top-level"
      pattern: "import core\\.models"
---

<objective>
Fix the lazy import defect in test_isolation.py that causes test_user_a_cannot_read_user_b_conversation_turns to fail when run in isolation.

Purpose: The isolation pen test currently depends on implicit test-collection side effects — other tests registering ConversationTurn in Base.metadata before db_session.create_all() runs. This is a defect: the test must be deterministic regardless of run order or test file selection.

Output: A single line added at module top-level — `import core.models` — which triggers the core/models/__init__.py import chain, registering all ORM models (including ConversationTurn / memory_conversations) in Base.metadata before any fixture executes.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tests/security/test_isolation.py
@backend/core/models/__init__.py
@backend/core/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lazy import defect in test_isolation.py</name>
  <files>backend/tests/security/test_isolation.py</files>
  <action>
    Add `import core.models  # noqa: F401 — registers all ORM models in Base.metadata` as a module-level import near the top of the file, after the existing imports from core.db.

    Root cause: `from memory.short_term import save_turn, load_recent_turns` is called inside the test function body at lines 85 and 131. When pytest runs test_isolation.py in isolation, the `db_session` fixture executes `Base.metadata.create_all()` before any test body runs. At that point, `ConversationTurn` (which registers the `memory_conversations` table) has not yet been imported, so `create_all` silently omits the table.

    Fix: Insert `import core.models  # noqa: F401` at module top level, immediately after the existing `from core.db import Base` import (line 28). `core/models/__init__.py` imports all ORM models including `ConversationTurn`, `UserCredential`, `Workflow`, `WorkflowRun`, and every other model — ensuring Base.metadata is fully populated before any fixture runs.

    Do NOT remove the lazy imports inside the test bodies (they are not harmful after this fix, just unnecessary). Do NOT change any test logic, assertions, or fixture code. The minimal correct change is adding exactly one import line.

    After the fix, the import section should look like:

    ```python
    from core.db import Base
    import core.models  # noqa: F401 — registers all ORM models in Base.metadata before db_session fixture
    ```

    This pattern is consistent with how Alembic's env.py ensures all models are registered before autogenerate runs.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/security/test_isolation.py -v --tb=short 2>&1 | tail -20</automated>
    <manual>Confirm output shows: 4 passed, 1 skipped — specifically that test_user_a_cannot_read_user_b_conversation_turns PASSED (not failed or errored)</manual>
    <sampling_rate>Run immediately after edit, before any other changes</sampling_rate>
  </verify>
  <done>
    Running `PYTHONPATH=. .venv/bin/pytest tests/security/test_isolation.py -v` standalone (not as part of full suite) shows 4 passed, 1 skipped. test_user_a_cannot_read_user_b_conversation_turns and test_user_a_cannot_read_user_b_conversation_turns_even_with_same_conversation_id both PASS. No "no such table: memory_conversations" error.
  </done>
</task>

</tasks>

<verification>
Run isolation tests standalone: `cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/security/test_isolation.py -v --tb=short`

Expected: 4 passed, 1 skipped (pgvector skip is intentional).

Also run full suite to confirm no regression: `PYTHONPATH=. .venv/bin/pytest tests/ -q`

Expected: 586+ tests pass (same or more than before this fix).
</verification>

<success_criteria>
- test_isolation.py runs cleanly in isolation: 4 passed, 1 skipped
- test_user_a_cannot_read_user_b_conversation_turns passes (was failing with "no such table: memory_conversations")
- Full test suite count unchanged or increased (no regressions)
- `grep "import core.models" backend/tests/security/test_isolation.py` returns a match
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-and-sandboxing/07-03-SUMMARY.md` with:
- What changed (the one-line import fix)
- Verification output (pytest stdout showing 4 passed, 1 skipped)
- Commit hash
</output>
