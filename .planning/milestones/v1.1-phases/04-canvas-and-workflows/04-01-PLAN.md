---
phase: 04-canvas-and-workflows
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/core/models/workflow.py
  - backend/core/models/__init__.py
  - backend/alembic/versions/009_phase4_workflows.py
  - backend/core/schemas/workflow.py
  - backend/api/routes/workflows.py
  - backend/main.py
  - frontend/src/app/workflows/page.tsx
  - frontend/src/app/workflows/[id]/page.tsx
  - frontend/src/app/api/workflows/route.ts
  - frontend/src/app/api/workflows/[id]/route.ts
  - frontend/src/app/api/workflows/[id]/run/route.ts
  - frontend/src/app/api/workflows/runs/[run_id]/events/route.ts
  - frontend/src/app/api/workflows/runs/[run_id]/approve/route.ts
  - frontend/src/app/api/workflows/runs/[run_id]/reject/route.ts
  - frontend/src/app/api/workflows/runs/pending-hitl/route.ts
  - frontend/src/app/api/workflows/templates/route.ts
  - frontend/src/app/api/workflows/templates/[id]/copy/route.ts
  - frontend/src/app/api/webhooks/[webhook_id]/route.ts
  - frontend/src/components/canvas/workflow-canvas.tsx
  - frontend/src/components/canvas/node-palette.tsx
  - frontend/src/hooks/use-workflow.ts
  - backend/tests/test_workflow_models.py
  - backend/tests/api/test_workflow_routes.py
autonomous: true
requirements:
  - WKFL-01
  - WKFL-08

must_haves:
  truths:
    - "Three tables exist in PostgreSQL: workflows, workflow_runs, workflow_triggers — created via Alembic migration 009"
    - "owner_user_id on workflows is NULLABLE — template rows have owner_user_id=NULL, regular workflows always have owner_user_id set"
    - "All workflow CRUD endpoints (GET/POST/PUT/DELETE /api/workflows) require JWT and pass 3-gate security"
    - "definition_json always has schema_version: '1.0' enforced at the Pydantic schema level"
    - "Webhook endpoint POST /api/webhooks/{webhook_id} accepts no JWT — validates X-Webhook-Secret header instead"
    - "Frontend canvas page at /workflows/[id] renders a React Flow canvas shell (empty, no execution yet)"
    - "Full test suite passes: 180+ passed, 0 failed"
  artifacts:
    - path: "backend/core/models/workflow.py"
      provides: "Workflow, WorkflowRun, WorkflowTrigger SQLAlchemy ORM models"
      contains: "class Workflow"
    - path: "backend/alembic/versions/009_phase4_workflows.py"
      provides: "Alembic migration creating workflows, workflow_runs, workflow_triggers tables"
      contains: "op.create_table('workflows'"
    - path: "backend/api/routes/workflows.py"
      provides: "CRUD + run + template + webhook endpoints"
      contains: "router = APIRouter"
    - path: "frontend/src/components/canvas/workflow-canvas.tsx"
      provides: "React Flow canvas shell (use client)"
      contains: "ReactFlow"
  key_links:
    - from: "backend/api/routes/workflows.py"
      to: "backend/main.py"
      via: "app.include_router(workflows_router, prefix='/api')"
      pattern: "include_router.*workflows"
    - from: "workflows table owner_user_id"
      to: "nullable — no FK to users table"
      via: "project rule: no FK constraints on user_id columns (users live in Keycloak)"
      pattern: "owner_user_id.*UUID.*nullable=True"
---

<objective>
Create the database tables, full CRUD API, webhook endpoint, and frontend canvas shell for Phase 4 workflows. No execution logic yet — that comes in 04-02 and 04-03.

Bug fix included: `owner_user_id` on the `workflows` table must be NULLABLE (not NOT NULL) so template rows inserted in 04-05 can have `owner_user_id=NULL`. The design doc incorrectly states `NOT NULL` — this plan corrects it. Per project rules there is no FK constraint on user_id columns (users live in Keycloak, not PostgreSQL).

Output: 3 new DB tables, full REST API (10 endpoints), Next.js proxy routes, canvas shell with React Flow installed.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-02-27-phase-4-canvas-workflows-design.md
@docs/plans/2026-02-27-phase-4-04-01-workflow-crud.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): SQLAlchemy ORM models — write failing tests</name>
  <files>
    backend/tests/test_workflow_models.py
  </files>
  <action>
    Create backend/tests/test_workflow_models.py with 3 failing import tests.
    Full test spec is in the referenced plan doc (Task 1). Key tests:
    - test_workflow_tablename: assert Workflow.__tablename__ == "workflows"
    - test_workflow_run_tablename: assert WorkflowRun.__tablename__ == "workflow_runs"
    - test_workflow_trigger_tablename: assert WorkflowTrigger.__tablename__ == "workflow_triggers"

    Run: cd backend && .venv/bin/pytest tests/test_workflow_models.py -v
    Expected: ImportError (models don't exist yet)
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/test_workflow_models.py -v 2>&1 | tail -8</automated>
    <manual>Must show FAILED or ERROR — not PASSED</manual>
    <sampling_rate>immediately after writing tests</sampling_rate>
  </verify>
  <done>
    - tests/test_workflow_models.py exists with 3 tests
    - All 3 fail with ImportError (RED state confirmed)
  </done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Create ORM models in backend/core/models/workflow.py</name>
  <files>
    backend/core/models/workflow.py
    backend/core/models/__init__.py
  </files>
  <action>
    Create backend/core/models/workflow.py with Workflow, WorkflowRun, WorkflowTrigger models.
    Full implementation spec in referenced plan doc (Task 1, Step 3).

    CRITICAL: owner_user_id on Workflow must be nullable=True (Column(UUID, nullable=True)).
    Template rows in 04-05 will have owner_user_id=NULL.
    DO NOT add a ForeignKey constraint — no FK on user_id columns per project rules.

    Workflow fields: id, owner_user_id (nullable!), name, description, definition_json (JSONB),
      is_template (default False), template_source_id (nullable FK to workflows.id), created_at, updated_at
    WorkflowRun fields: id, workflow_id (FK), owner_user_id, trigger_type, status, checkpoint_id,
      started_at, completed_at, result_json
    WorkflowTrigger fields: id, workflow_id (FK), owner_user_id, trigger_type, cron_expression,
      webhook_secret, is_active (default True)

    Update backend/core/models/__init__.py to export all three models.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/test_workflow_models.py -v 2>&1 | tail -8</automated>
    <manual>All 3 tests PASSED</manual>
    <sampling_rate>after creating workflow.py</sampling_rate>
  </verify>
  <done>
    - backend/core/models/workflow.py exists with all 3 models
    - owner_user_id is nullable on Workflow
    - test_workflow_models.py: 3 PASSED
  </done>
</task>

<task type="tdd">
  <name>Task 3: Alembic migration 009_phase4_workflows</name>
  <files>
    backend/alembic/versions/009_phase4_workflows.py
  </files>
  <action>
    Create Alembic migration 009_phase4_workflows.py.
    Full spec in referenced plan doc (Task 2).

    Migration creates 3 tables. owner_user_id on workflows must be NULLABLE (no NOT NULL constraint).
    down_revision must reference the current head (check with: cd backend && .venv/bin/alembic heads).

    Do NOT run the migration — document the command but do not execute it:
    just migrate  (or: docker exec -it blitz-postgres psql -U blitz blitz -c "...")
    Migration runs against live Docker PostgreSQL — document the apply command in a comment at top of file.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/alembic check 2>&1 | tail -5</automated>
    <manual>alembic check should show migration is pending (not yet applied). File exists and is valid Python.</manual>
    <sampling_rate>after creating the migration file</sampling_rate>
  </verify>
  <done>
    - backend/alembic/versions/009_phase4_workflows.py exists
    - owner_user_id column has nullable=True / no NOT NULL constraint
    - alembic check reports the migration as pending
  </done>
</task>

<task type="tdd">
  <name>Task 4 (RED→GREEN): Pydantic schemas + FastAPI routes</name>
  <files>
    backend/core/schemas/workflow.py
    backend/api/routes/workflows.py
    backend/tests/api/test_workflow_routes.py
    backend/main.py
  </files>
  <action>
    Full TDD spec in referenced plan doc (Tasks 3–5).

    1. Write failing tests in backend/tests/api/test_workflow_routes.py (RED)
    2. Create backend/core/schemas/workflow.py — WorkflowCreate, WorkflowUpdate, WorkflowResponse,
       WorkflowRunResponse, WorkflowTriggerCreate, WorkflowTriggerResponse Pydantic v2 models.
       WorkflowCreate must validate that definition_json contains schema_version == "1.0".
    3. Create backend/api/routes/workflows.py with all 10+ endpoints following existing route
       patterns (security/deps.py get_current_user, get_db).
    4. Register router in backend/main.py.

    Key endpoints:
    - GET/POST /api/workflows (list user's, create)
    - GET/PUT/DELETE /api/workflows/{id}
    - GET /api/workflows/templates + POST /api/workflows/templates/{id}/copy
    - POST /api/workflows/{id}/run → returns {run_id}
    - GET /api/workflows/runs/{run_id}/events (SSE stub — returns empty stream for now)
    - GET /api/workflows/runs/{run_id}
    - POST /api/workflows/runs/{run_id}/approve + /reject
    - GET /api/workflows/runs/pending-hitl
    - GET/POST/DELETE /api/workflows/{id}/triggers
    - POST /api/webhooks/{webhook_id} (public — no JWT, validates X-Webhook-Secret)
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_workflow_routes.py -v 2>&1 | tail -15</automated>
    <manual>All route tests PASSED</manual>
    <sampling_rate>after each file</sampling_rate>
  </verify>
  <done>
    - backend/core/schemas/workflow.py: Pydantic v2 models with schema_version validation
    - backend/api/routes/workflows.py: all endpoints registered
    - backend/main.py: includes workflows_router
    - test_workflow_routes.py: all tests PASSED
  </done>
</task>

<task type="tdd">
  <name>Task 5: Frontend — Next.js proxy routes + canvas shell</name>
  <files>
    frontend/src/app/workflows/page.tsx
    frontend/src/app/workflows/[id]/page.tsx
    frontend/src/app/api/workflows/route.ts
    frontend/src/app/api/workflows/[id]/route.ts
    frontend/src/app/api/workflows/[id]/run/route.ts
    frontend/src/app/api/workflows/runs/[run_id]/events/route.ts
    frontend/src/app/api/workflows/runs/[run_id]/approve/route.ts
    frontend/src/app/api/workflows/runs/[run_id]/reject/route.ts
    frontend/src/app/api/workflows/runs/pending-hitl/route.ts
    frontend/src/app/api/workflows/templates/route.ts
    frontend/src/app/api/workflows/templates/[id]/copy/route.ts
    frontend/src/app/api/webhooks/[webhook_id]/route.ts
    frontend/src/components/canvas/workflow-canvas.tsx
    frontend/src/components/canvas/node-palette.tsx
    frontend/src/hooks/use-workflow.ts
  </files>
  <action>
    Full spec in referenced plan doc (Tasks 6–8).

    1. Install @xyflow/react if not already present: cd frontend && pnpm add @xyflow/react
    2. Create all Next.js proxy routes (follow pattern from existing /api/conversations/route.ts —
       inject JWT from getServerSession(authOptions), proxy to NEXT_PUBLIC_API_URL).
    3. workflows/page.tsx — Server Component: list user workflows + template gallery placeholder.
    4. workflows/[id]/page.tsx — Server Component shell containing WorkflowCanvas (Client Component).
    5. workflow-canvas.tsx — "use client", renders <ReactFlow> with empty nodes/edges.
       No execution wiring yet — just the canvas visible.
    6. node-palette.tsx — "use client", left sidebar with 6 node type entries (drag sources).
    7. use-workflow.ts — CRUD hook (list, create, update, delete) using fetch to /api/workflows proxy.

    SSE events/route.ts: for now, just proxy the backend SSE stream through (ReadableStream passthrough).
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build 2>&1 | tail -20</automated>
    <manual>Build succeeds with no TypeScript errors. /workflows page renders in browser (just list). /workflows/[id] renders empty canvas.</manual>
    <sampling_rate>after all frontend files created</sampling_rate>
  </verify>
  <done>
    - pnpm run build: 0 TypeScript errors
    - /workflows page accessible
    - /workflows/[id] shows React Flow canvas shell
    - All proxy route files exist
  </done>
</task>

<task type="auto">
  <name>Task 6: Full suite green</name>
  <files></files>
  <action>
    Run the full backend test suite and confirm 0 failures.

    cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q

    If failures: investigate root cause. Do not skip or xfail.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q 2>&1 | tail -5</automated>
    <manual>N passed, 0 failed. N >= 185 (180 existing + new workflow tests)</manual>
    <sampling_rate>after all tasks complete</sampling_rate>
  </verify>
  <done>
    Full suite: N passed, 0 failed, 0 errors.
  </done>
</task>

</tasks>

<verification>
Plan 04-01 is complete when:

1. **WKFL-08**: `definition_json` validation in WorkflowCreate Pydantic schema enforces `schema_version == "1.0"`
2. **WKFL-01 partial**: React Flow canvas renders at `/workflows/[id]` (full drag-and-drop in 04-04)
3. **DB**: `just migrate` runs cleanly against the PostgreSQL container (or migration verified via alembic check)
4. **API**: All workflow CRUD endpoints respond correctly (unit tests pass)
5. **Full suite**: `pytest tests/ -q` shows 0 failures

Automated gates:
```bash
cd /home/tungmv/Projects/hox-agentos/backend
PYTHONPATH=. .venv/bin/pytest tests/test_workflow_models.py tests/api/test_workflow_routes.py -v
PYTHONPATH=. .venv/bin/pytest tests/ -q
cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build
```
</verification>

<success_criteria>
- backend/core/models/workflow.py: Workflow.owner_user_id is nullable (templates can be NULL)
- backend/alembic/versions/009_phase4_workflows.py: migration creates all 3 tables
- backend/api/routes/workflows.py: all 10+ endpoints registered and protected by JWT/ACL
- backend/core/schemas/workflow.py: schema_version == "1.0" enforced at validation
- POST /api/webhooks/{webhook_id}: public endpoint, validates X-Webhook-Secret
- frontend/src/components/canvas/workflow-canvas.tsx: renders ReactFlow
- pnpm run build: 0 TypeScript errors
- pytest tests/ -q: 0 failures
</success_criteria>

<output>
After completion, create .planning/phases/04-canvas-and-workflows/04-01-SUMMARY.md following the summary template.

Update .planning/ROADMAP.md: mark plan 04-01 as in-progress or complete in the Phase 4 section.
</output>
