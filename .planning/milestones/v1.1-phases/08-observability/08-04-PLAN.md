---
phase: 08-observability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements: [OBS-07]
gap_closure: true

must_haves:
  truths:
    - "Ops users authenticated via Keycloak SSO can access Grafana Explore"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Grafana OAuth role mapping granting Editor to ops role"
      contains: "contains(realm_roles[*], 'ops') && 'Editor'"
  key_links:
    - from: "docker-compose.yml GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH"
      to: "Grafana Explore access"
      via: "Grafana 11 role-based feature gates (Editor+ required for Explore)"
      pattern: "'ops'.*'Editor'"
---

<objective>
Fix Grafana OAuth role mapping so ops users are granted Editor (not Viewer) when they log in via Keycloak SSO. Grafana 11 requires Editor or higher to access Explore; the current Viewer mapping silently hides the Explore sidebar entry for all ops users.

Purpose: Close the UAT gap from test 7 — ops users must be able to reach Explore and run Loki queries without switching to the local admin account.
Output: Updated docker-compose.yml with a one-character role change; Grafana container restarted.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/tungmv/Projects/hox-agentos/.planning/PROJECT.md
@/home/tungmv/Projects/hox-agentos/.planning/ROADMAP.md
@/home/tungmv/Projects/hox-agentos/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change ops role mapping from Viewer to Editor in docker-compose.yml</name>
  <files>/home/tungmv/Projects/hox-agentos/docker-compose.yml</files>
  <action>
    In docker-compose.yml, find the grafana service environment block (around line 245).

    Locate this exact line:
      GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH: "contains(realm_roles[*], 'admin') && 'Admin' || contains(realm_roles[*], 'ops') && 'Viewer' || 'Viewer'"

    Replace it with:
      GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH: "contains(realm_roles[*], 'admin') && 'Admin' || contains(realm_roles[*], 'ops') && 'Editor' || 'Viewer'"

    The only change is 'Viewer' → 'Editor' for the ops branch. The final fallback 'Viewer' (for users with no recognized role) stays unchanged.

    After editing, restart only the grafana service so the new env var takes effect:
      docker compose restart grafana

    Wait for grafana to be healthy (check with: docker compose ps grafana).
    Grafana persists its grafana_data volume — no data loss on restart.
  </action>
  <verify>
    <automated>docker compose ps grafana | grep -E "(running|healthy)"</automated>
    <manual>
      1. Open http://localhost:3001 in a browser.
      2. Click "Sign in with Keycloak" (or equivalent SSO button).
      3. Log in with a Keycloak user that has the ops role (NOT the local admin/admin account).
      4. After login, confirm the left sidebar shows the Explore item (compass icon).
      5. Click Explore, select datasource "blitz-loki", run query {job="blitz-agentos"}.
      6. Log entries appear (expect ~517 entries similar to admin test).
    </manual>
    <sampling_rate>run after this task commits, before writing SUMMARY</sampling_rate>
  </verify>
  <done>
    Keycloak ops-role users see Explore in the Grafana sidebar and can successfully run Loki queries. The local admin account retains Admin-level access (unchanged). Users with no known role retain Viewer (unchanged, cannot access Explore — intentional).
  </done>
</task>

</tasks>

<verification>
- docker-compose.yml contains 'ops') && 'Editor' (not 'Viewer') in ROLE_ATTRIBUTE_PATH
- Grafana container is running/healthy after restart
- Ops-role SSO login shows Explore in sidebar (human verify)
</verification>

<success_criteria>
UAT test 7 passes: Grafana Explore is visible and functional for Keycloak ops-role users. Phase 8 UAT goes from 8/9 to 9/9.
</success_criteria>

<output>
After completion, create `/home/tungmv/Projects/hox-agentos/.planning/phases/08-observability/08-04-SUMMARY.md`
</output>
