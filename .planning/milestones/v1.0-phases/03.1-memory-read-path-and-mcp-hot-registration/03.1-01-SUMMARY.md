---
phase: 03.1-memory-read-path-and-mcp-hot-registration
plan: 01
subsystem: memory
tags: [memory, mcp, langgraph, medium-term, episode, registry, tdd]

# Dependency graph
requires:
  - phase: 03-sub-agents-memory-and-integrations
    provides: "load_recent_episodes() in memory/medium_term.py, MCPToolRegistry.refresh() in mcp/registry.py — both fully implemented but unconnected"
provides:
  - "Episode summaries from medium-term memory injected as SystemMessage('[Medium-term memory...') in _load_memory_node"
  - "MCPToolRegistry.refresh() called after create_mcp_server() commit — new MCP servers hot-registered without restart"
  - "3 new tests: MEMO-02 (2 tests) and INTG-01 (1 test) — all passing"
affects:
  - "Phase 4 (Canvas & Workflows) — agents now surface past conversation context across sessions"
  - "Phase 5 (Channels) — MCP tool availability after admin registration is immediate"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Top-level import required for patchability: load_recent_episodes imported at module top (not lazy) so patch('agents.master_agent.load_recent_episodes') works"
    - "Lazy import for circular-import avoidance: MCPToolRegistry imported inside create_mcp_server() function body; patched via 'mcp.registry.MCPToolRegistry'"
    - "Episode ordering: episodes inserted at history[0] AFTER long-term facts block — broader context (episodes) before specific facts"

key-files:
  created: []
  modified:
    - "backend/agents/master_agent.py"
    - "backend/api/routes/mcp_servers.py"
    - "backend/tests/agents/test_master_agent_memory.py"
    - "backend/tests/mcp/test_mcp_registry.py"

key-decisions:
  - "[03.1-01]: load_recent_episodes imported at top level in master_agent.py (not lazy) — lazy imports not patchable with patch('agents.master_agent.load_recent_episodes'); same pattern as call_mcp_tool in project_agent.py"
  - "[03.1-01]: MCPToolRegistry imported lazily inside create_mcp_server() — avoids circular import between api.routes.mcp_servers and mcp.registry; test patches 'mcp.registry.MCPToolRegistry'"
  - "[03.1-01]: Episode injection uses state['messages']=[] path for testing — skip_short_term=False means full history (with injected SystemMessage) returned in result['messages']"

patterns-established:
  - "TDD RED/GREEN: failing tests committed first (0bb22b4), implementation second (31baeb2)"
  - "Graceful degradation: try/except around episode load — failure logs warning but never blocks agent"
  - "Memory ordering: episodes (broader, cross-session) before long-term facts (query-specific) before conversation turns"

requirements-completed:
  - MEMO-02
  - INTG-01

# Metrics
duration: 4min
completed: 2026-02-26
---

# Phase 3.1 Plan 01: Memory Read Path and MCP Hot-Registration Summary

**Episode summaries wired into _load_memory_node as SystemMessage prefix via load_recent_episodes(), and MCPToolRegistry.refresh() called after create_mcp_server() commit for immediate tool availability — all verified with 3 new TDD tests, full suite 180 passed**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-26T16:32:50Z
- **Completed:** 2026-02-26T16:37:29Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- MEMO-02 closed: `_load_memory_node` now injects medium-term episode summaries as `[Medium-term memory — summaries of past conversations:]` SystemMessage at `history[0]`, loaded unconditionally when `user_id` is present, with graceful try/except so episode DB failures never block the agent
- INTG-01 closed: `create_mcp_server()` calls `await MCPToolRegistry.refresh()` after `session.commit()` via lazy import inside function body, making newly registered MCP servers immediately callable without backend restart
- 3 new TDD tests: `test_load_memory_node_injects_episodes_as_system_message`, `test_load_memory_node_gracefully_handles_episode_failure`, `test_create_mcp_server_calls_refresh_after_commit` — all GREEN; full suite 180 passed, 0 failures

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Write failing tests** - `0bb22b4` (test)
2. **Task 2 (GREEN): Wire episode injection + MCP hot-registration** - `31baeb2` (feat)
3. **Task 3: Full suite green + VALIDATION.md updated** - `25c7ad0` (chore)

_TDD plan: RED commit first, GREEN commit second, as intended._

## Files Created/Modified

- `backend/agents/master_agent.py` — Added `from memory.medium_term import load_recent_episodes` top-level import; added episode injection block (lines after long-term facts except block, before `if skip_short_term:`)
- `backend/api/routes/mcp_servers.py` — Added lazy `from mcp.registry import MCPToolRegistry` + `await MCPToolRegistry.refresh()` inside `create_mcp_server()` after `session.refresh(server)`
- `backend/tests/agents/test_master_agent_memory.py` — Added 2 new async tests for episode injection happy path and graceful failure
- `backend/tests/mcp/test_mcp_registry.py` — Added 1 new async test for MCP hot-registration after commit

## Decisions Made

- `load_recent_episodes` imported at top level (not lazy) to maintain patchability via `patch("agents.master_agent.load_recent_episodes")` — follows same pattern as `call_mcp_tool` in `project_agent.py` (decision from Phase 03-04)
- `MCPToolRegistry` imported lazily inside `create_mcp_server()` function body to avoid circular import between `api.routes.mcp_servers` and `mcp.registry`; test patches `mcp.registry.MCPToolRegistry` (the class definition module) which correctly intercepts the lazy import
- Episode injection test uses `state["messages"] = []` (skip_short_term=False path) so full history including injected SystemMessage is returned in `result["messages"]` — this is the only way to observe the injection in tests

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The plan's notes about import patchability and lazy vs. eager import tradeoffs were precise — no surprises during implementation.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- MEMO-02 and INTG-01 requirements from the 3.1 audit are now closed
- Phase 3.1 gap-closure complete: full suite green at 180 tests
- Phase 3 is fully complete; Phase 4 (Canvas & Workflows) can proceed

---
*Phase: 03.1-memory-read-path-and-mcp-hot-registration*
*Completed: 2026-02-26*
