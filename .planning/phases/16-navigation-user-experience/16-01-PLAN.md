---
phase: 16-navigation-user-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models/user_preferences.py
  - backend/core/models/__init__.py
  - backend/api/routes/user_preferences.py
  - backend/main.py
  - backend/alembic/versions/020_add_user_preferences.py
  - backend/tests/api/test_user_preferences.py
autonomous: true
requirements:
  - NAV-10
  - NAV-07
  - NAV-08

must_haves:
  truths:
    - "GET /api/users/me/preferences returns current user's preferences (thinking_mode, response_style) with defaults"
    - "PUT /api/users/me/preferences persists new preference values and returns updated state"
    - "Preferences are scoped to the authenticated user via JWT — no cross-user access"
    - "user_preferences table exists with JSONB preferences column"
  artifacts:
    - path: "backend/core/models/user_preferences.py"
      provides: "UserPreferences SQLAlchemy ORM model"
      contains: "class UserPreferences"
    - path: "backend/api/routes/user_preferences.py"
      provides: "GET/PUT /api/users/me/preferences endpoints"
      exports: ["router"]
    - path: "backend/alembic/versions/020_add_user_preferences.py"
      provides: "Migration creating user_preferences table"
      contains: "user_preferences"
    - path: "backend/tests/api/test_user_preferences.py"
      provides: "Tests for preference endpoints"
      min_lines: 50
  key_links:
    - from: "backend/api/routes/user_preferences.py"
      to: "backend/core/models/user_preferences.py"
      via: "SQLAlchemy query"
      pattern: "UserPreferences"
    - from: "backend/main.py"
      to: "backend/api/routes/user_preferences.py"
      via: "router include"
      pattern: "user_preferences"
---

<objective>
Create the backend infrastructure for user preferences: database model, Alembic migration, and REST API endpoints.

Purpose: NAV-07, NAV-08, NAV-10 require a `user_preferences` table and API so the frontend profile page can read/write LLM preferences (thinking mode on/off, response style concise/detailed/conversational). This plan creates the backend foundation that Plan 03 (profile page) will consume.

Output: `UserPreferences` ORM model, migration 020, `GET/PUT /api/users/me/preferences` endpoints, and tests.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-navigation-user-experience/16-CONTEXT.md

<interfaces>
<!-- Existing patterns the executor needs -->

From backend/core/models/user_instructions.py:
```python
class UserInstructions(Base):
    __tablename__ = "user_instructions"
    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), nullable=False, unique=True, index=True)
    instructions: Mapped[str] = mapped_column(Text, nullable=False, default="")
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, server_default=func.now(), onupdate=func.now())
```

From backend/core/models/user.py:
```python
class UserContext(TypedDict):
    user_id: UUID
    email: str
    username: str
    roles: list[str]
    groups: list[str]
```

From backend/security/deps.py:
```python
async def get_current_user(...) -> UserContext: ...
async def get_user_db(...) -> AsyncGenerator[AsyncSession, None]: ...
```

From backend/api/routes/user_instructions.py (pattern to follow):
```python
router = APIRouter(prefix="/user/instructions", tags=["user"])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UserPreferences model and migration</name>
  <files>backend/core/models/user_preferences.py, backend/core/models/__init__.py, backend/alembic/versions/020_add_user_preferences.py</files>
  <behavior>
    - UserPreferences model has id (UUID PK), user_id (UUID unique indexed, no FK), preferences (JSONB with SQLite variant), created_at, updated_at
    - JSONB column uses `JSON().with_variant(JSONB(), 'postgresql')` pattern for SQLite test compat (per CLAUDE.md gotcha)
    - No FK on user_id (users live in Keycloak, not PostgreSQL — per CLAUDE.md constraint)
    - Default preferences: `{"thinking_mode": false, "response_style": "concise"}`
  </behavior>
  <action>
    Create `backend/core/models/user_preferences.py` with `UserPreferences` SQLAlchemy model:
    - `id`: UUID primary key with default uuid4
    - `user_id`: UUID, unique=True, index=True, nullable=False (NO foreign key — users are in Keycloak)
    - `preferences`: JSONB column using `JSON().with_variant(JSONB(), 'postgresql')` for SQLite test compat
    - `created_at`, `updated_at`: DateTime(timezone=True) with server_default=func.now(), onupdate pattern

    Follow the exact same structure as `user_instructions.py` model. Import the model in `core/models/__init__.py` so Alembic autogenerate picks it up.

    Create Alembic migration 020: `cd backend && .venv/bin/alembic revision --autogenerate -m "add_user_preferences"`. The migration should create the `user_preferences` table. If autogenerate doesn't produce a clean migration (due to JSONB variant), write it manually with `op.create_table(...)`. Ensure the revision chain is: 019 -> 020 (linear, no branching).
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. python -c "from core.models.user_preferences import UserPreferences; print('model OK')" && PYTHONPATH=. .venv/bin/pytest tests/ -q -x</automated>
  </verify>
  <done>UserPreferences model exists with JSONB preferences column, migration 020 creates user_preferences table, existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create preference API endpoints and tests</name>
  <files>backend/api/routes/user_preferences.py, backend/main.py, backend/tests/api/test_user_preferences.py</files>
  <behavior>
    - GET /api/users/me/preferences returns {"thinking_mode": false, "response_style": "concise"} when no preferences exist (defaults)
    - GET /api/users/me/preferences returns stored preferences when row exists
    - PUT /api/users/me/preferences with {"thinking_mode": true, "response_style": "detailed"} persists and returns updated values
    - PUT validates response_style is one of: concise, detailed, conversational
    - PUT validates thinking_mode is boolean
    - Unauthenticated request returns 401
  </behavior>
  <action>
    Create `backend/api/routes/user_preferences.py`:
    - `router = APIRouter(prefix="/users/me/preferences", tags=["user"])`
    - Pydantic schemas:
      - `UserPreferencesResponse(BaseModel)`: thinking_mode: bool, response_style: Literal["concise", "detailed", "conversational"]
      - `UserPreferencesUpdate(BaseModel)`: thinking_mode: bool | None = None, response_style: Literal["concise", "detailed", "conversational"] | None = None (partial update)
    - `GET /` endpoint: query UserPreferences by user_id from JWT. If no row, return defaults `{"thinking_mode": false, "response_style": "concise"}`. If row exists, parse JSONB and return.
    - `PUT /` endpoint: upsert — if row exists, merge new values into existing JSONB. If not, create row with provided values merged with defaults. Return updated preferences.
    - Export a helper function `get_user_preference_values(user_id: UUID, session: AsyncSession) -> dict` that Plan 03 will import to inject into agent prompts.
    - Use `Depends(get_current_user)` and `Depends(get_user_db)` — same pattern as user_instructions.py.
    - Use structlog for logging (never log credential values).

    Register the router in `backend/main.py`: `from api.routes.user_preferences import router as user_preferences_router` and `app.include_router(user_preferences_router, prefix="/api")`.

    Create `backend/tests/api/test_user_preferences.py` with tests:
    - test_get_preferences_default (no row exists -> returns defaults)
    - test_put_preferences_creates_row (upsert creates new)
    - test_put_preferences_updates_existing (upsert updates)
    - test_put_preferences_partial_update (only thinking_mode sent -> response_style unchanged)
    - test_put_invalid_response_style (422 on bad value)

    Follow existing test patterns from the codebase (use the same conftest fixtures for auth mocking).
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_user_preferences.py -v</automated>
  </verify>
  <done>GET /api/users/me/preferences returns defaults or stored prefs, PUT upserts and validates, all 5+ tests pass, router registered in main.py</done>
</task>

</tasks>

<verification>
- `cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q` — full suite passes (no regressions)
- `cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_user_preferences.py -v` — all preference tests pass
- `.venv/bin/alembic heads` shows single head at 020
</verification>

<success_criteria>
- user_preferences table defined with JSONB column and migration 020
- GET endpoint returns defaults when no preferences exist
- PUT endpoint validates and persists thinking_mode (bool) and response_style (enum)
- Helper function exported for agent prompt injection
- All tests pass including existing suite
</success_criteria>

<output>
After completion, create `.planning/phases/16-navigation-user-experience/16-01-SUMMARY.md`
</output>