---
phase: 16-navigation-user-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/(authenticated)/layout.tsx
  - frontend/src/components/nav-rail.tsx
  - frontend/src/components/mobile-tab-bar.tsx
  - frontend/src/app/(authenticated)/chat/page.tsx
  - frontend/src/app/(authenticated)/workflows/page.tsx
  - frontend/src/app/(authenticated)/workflows/new/page.tsx
  - frontend/src/app/(authenticated)/workflows/[id]/page.tsx
  - frontend/src/app/(authenticated)/settings/page.tsx
  - frontend/src/app/(authenticated)/settings/channels/page.tsx
  - frontend/src/app/(authenticated)/settings/memory/page.tsx
  - frontend/src/app/(authenticated)/settings/chat-preferences/page.tsx
  - frontend/src/app/(authenticated)/settings/integrations/page.tsx
  - frontend/src/app/(authenticated)/settings/agents/page.tsx
  - frontend/src/app/(authenticated)/admin/layout.tsx
  - frontend/src/app/(authenticated)/admin/page.tsx
  - frontend/src/app/(authenticated)/profile/page.tsx
  - frontend/src/components/chat/conversation-sidebar.tsx
  - frontend/package.json
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04

must_haves:
  truths:
    - "A 64px dark vertical navigation rail with icons is visible on all authenticated pages"
    - "The nav rail has Chat, Workflows, Skills in the top group and Admin, Settings, Profile in the bottom group"
    - "The Admin nav item is visible only to users with admin, developer, or it-admin roles"
    - "Active nav item has a blue left border indicator and lighter background"
    - "The /login page and API routes have NO nav rail"
    - "Clicking an avatar at the bottom opens a dropdown with Profile link and Sign Out button"
    - "On mobile (below md breakpoint), a bottom tab bar replaces the nav rail"
  artifacts:
    - path: "frontend/src/app/(authenticated)/layout.tsx"
      provides: "Route group layout wrapping all authenticated pages with NavRail + MobileTabBar"
      min_lines: 20
    - path: "frontend/src/components/nav-rail.tsx"
      provides: "Dark sidebar navigation rail component"
      min_lines: 60
    - path: "frontend/src/components/mobile-tab-bar.tsx"
      provides: "Bottom tab bar for mobile screens"
      min_lines: 30
  key_links:
    - from: "frontend/src/app/(authenticated)/layout.tsx"
      to: "frontend/src/components/nav-rail.tsx"
      via: "import and render"
      pattern: "NavRail"
    - from: "frontend/src/app/(authenticated)/layout.tsx"
      to: "frontend/src/components/mobile-tab-bar.tsx"
      via: "import and render"
      pattern: "MobileTabBar"
    - from: "frontend/src/components/nav-rail.tsx"
      to: "next-auth/react"
      via: "useSession for role check"
      pattern: "useSession"
---

<objective>
Create the persistent navigation rail, mobile tab bar, and restructure frontend routes into an `(authenticated)` route group so all authenticated pages share the nav layout.

Purpose: NAV-01 (nav rail visible on all auth pages), NAV-02 (admin role gating), NAV-03 (active indicator + avatar dropdown), NAV-04 (route group exclusion of /login and API routes). This is the core UX restructuring — every other authenticated page will live inside this layout.

Output: NavRail component, MobileTabBar component, `(authenticated)/layout.tsx` route group, all existing pages moved into the route group.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-navigation-user-experience/16-CONTEXT.md

<interfaces>
<!-- Existing patterns the executor needs -->

From frontend/src/app/layout.tsx (root — NOT changing):
```tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <SessionProvider refetchOnWindowFocus={true} refetchInterval={300}>
          {children}
          <AuthErrorToasts />
        </SessionProvider>
      </body>
    </html>
  );
}
```

From frontend/src/types/next-auth.d.ts:
```typescript
declare module "next-auth" {
  interface Session {
    accessToken?: string;
    realmRoles?: string[];
    error?: string;
    idToken?: string;
    authProvider?: "keycloak" | "credentials";
  }
}
```

From frontend/src/app/admin/layout.tsx (RBAC pattern to reuse):
```typescript
const ADMIN_ROLES = ["it-admin", "admin", "developer"];
// ... role check via session.realmRoles
```

From frontend/src/components/sign-out-button.tsx:
```typescript
export function SignOutButton() { ... }
// Handles both Keycloak end-session and local auth logout
```

Current page structure to move:
- app/chat/page.tsx -> app/(authenticated)/chat/page.tsx
- app/workflows/ -> app/(authenticated)/workflows/
- app/settings/ -> app/(authenticated)/settings/
- app/admin/ -> app/(authenticated)/admin/
- (NEW) app/(authenticated)/profile/page.tsx (placeholder for Plan 03)

Pages that stay OUTSIDE (authenticated) route group:
- app/login/page.tsx (public)
- app/api/ (API routes)
- app/page.tsx (root redirect)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and create NavRail + MobileTabBar components</name>
  <files>frontend/package.json, frontend/src/components/nav-rail.tsx, frontend/src/components/mobile-tab-bar.tsx</files>
  <action>
    **Install lucide-react** (icon library):
    ```bash
    cd /home/tungmv/Projects/hox-agentos/frontend && pnpm add lucide-react
    ```

    **Create `frontend/src/components/nav-rail.tsx`** — Client Component ("use client"):

    Visual design (per CONTEXT.md locked decisions):
    - Dark sidebar: charcoal background (~`bg-gray-900` or `#1a1a2e` / `#1e1e2e` — Linear/VS Code activity bar feel)
    - Fixed 64px width: `w-16`
    - Full viewport height: `h-screen fixed left-0 top-0`
    - Hidden on mobile: `hidden md:flex flex-col`
    - "B" monogram logo at top in a rounded-lg container with accent color background
    - Active indicator: `border-l-[3px] border-blue-500 bg-white/10` on the active item

    Nav items structure:
    - Top group (flex-1): Chat (MessageSquare icon), Workflows (GitBranch icon), Skills (Zap icon)
    - Bottom group: Admin (Shield icon, role-gated), Settings (Settings icon), Profile avatar area
    - Each item: icon centered, tooltip on hover using `title` attribute or a simple absolute-positioned tooltip div (discretion)
    - Use `usePathname()` from `next/navigation` to determine active state (match on `/chat`, `/workflows`, `/settings`, `/admin`, `/profile`)

    Admin role gating (NAV-02):
    - Use `useSession()` to get session
    - Extract `realmRoles` from session (check `session.realmRoles` — already exposed per next-auth.d.ts)
    - Show Admin item only if `realmRoles` includes "admin", "developer", or "it-admin"

    Avatar dropdown (NAV-03):
    - At the bottom, show a small avatar circle (user initial from session.user.name or email)
    - Click opens a dropdown with: "Profile" link (`/profile`), divider, `<SignOutButton />`
    - Click outside closes dropdown (simple state toggle)

    **Create `frontend/src/components/mobile-tab-bar.tsx`** — Client Component ("use client"):
    - Fixed bottom bar: `fixed bottom-0 left-0 right-0 md:hidden`
    - 5 items: Chat, Workflows, Skills, Settings, Profile (no Admin tab — per CONTEXT.md, admin access on mobile is via profile page link)
    - Each: icon + small label text below
    - Active state: blue icon + blue text vs gray
    - Use `usePathname()` for active detection
    - Background: white with top border, slight shadow

    Icons to use from lucide-react:
    - Chat: `MessageSquare`
    - Workflows: `GitBranch`
    - Skills: `Zap`
    - Settings: `Settings`
    - Admin: `Shield`
    - Profile: `User`
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>NavRail renders dark sidebar with icon items, role-gated admin, avatar dropdown with SignOut. MobileTabBar renders 5-item bottom bar. Both compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create (authenticated) route group and move existing pages</name>
  <files>frontend/src/app/(authenticated)/layout.tsx, frontend/src/app/(authenticated)/chat/page.tsx, frontend/src/app/(authenticated)/workflows/page.tsx, frontend/src/app/(authenticated)/workflows/new/page.tsx, frontend/src/app/(authenticated)/workflows/[id]/page.tsx, frontend/src/app/(authenticated)/settings/page.tsx, frontend/src/app/(authenticated)/settings/channels/page.tsx, frontend/src/app/(authenticated)/settings/memory/page.tsx, frontend/src/app/(authenticated)/settings/chat-preferences/page.tsx, frontend/src/app/(authenticated)/settings/integrations/page.tsx, frontend/src/app/(authenticated)/settings/agents/page.tsx, frontend/src/app/(authenticated)/admin/layout.tsx, frontend/src/app/(authenticated)/admin/page.tsx, frontend/src/app/(authenticated)/profile/page.tsx, frontend/src/components/chat/conversation-sidebar.tsx</files>
  <action>
    **Create `frontend/src/app/(authenticated)/layout.tsx`:**
    - This is a Server Component (no "use client" needed — children are passed through)
    - Layout structure:
      ```tsx
      <div className="flex min-h-screen">
        <NavRail />
        <main className="flex-1 md:ml-16">  {/* offset by nav rail width on desktop */}
          <div className="pb-16 md:pb-0">   {/* bottom padding for mobile tab bar */}
            {children}
          </div>
        </main>
        <MobileTabBar />
      </div>
      ```
    - Import NavRail and MobileTabBar components

    **Move existing pages** into `(authenticated)/` route group:
    Using `git mv` to preserve history:
    ```bash
    cd /home/tungmv/Projects/hox-agentos/frontend/src/app
    mkdir -p "(authenticated)"
    git mv chat "(authenticated)/chat"
    git mv workflows "(authenticated)/workflows"
    git mv settings "(authenticated)/settings"
    git mv admin "(authenticated)/admin"
    ```

    **Create profile placeholder** at `(authenticated)/profile/page.tsx`:
    - Simple Server Component that displays "Profile page coming soon" — Plan 03 will implement the full page
    - This is needed so the nav rail Profile link has a destination

    **Update conversation-sidebar.tsx:**
    - Remove the Settings link and SignOutButton from the sidebar footer — these now live in the nav rail
    - Keep the conversation list and "New Conversation" button unchanged
    - The footer section (`border-t`) can be removed entirely or simplified

    **Important: Do NOT touch these files:**
    - `app/layout.tsx` (root layout — stays unchanged)
    - `app/login/page.tsx` (public — stays outside route group)
    - `app/page.tsx` (root redirect — stays outside route group)
    - `app/api/` (API routes — stays outside route group)

    **Import path check:** After moving, verify all `@/` imports still resolve correctly. Next.js `(authenticated)` is a route group — it does NOT affect the URL path. `/chat` still maps to `/(authenticated)/chat/page.tsx`. The `@/components/...` and `@/hooks/...` imports are unaffected because they reference `src/components/` and `src/hooks/` which are not moved.

    **CRITICAL: Also move all admin sub-pages.** The admin directory has sub-pages (agents, tools, skills, mcp-servers, permissions, config, credentials, users, skill-store, create). Use `git mv admin "(authenticated)/admin"` to move the entire directory tree at once.

    **After moving, verify the `_pending-badge.tsx` component** in workflows/ is also moved (it's a non-page component file in the workflows directory).
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>All authenticated pages live under (authenticated)/ route group with shared NavRail layout. /login and API routes are excluded. Conversation sidebar no longer shows Settings/SignOut (nav rail handles those). TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit` — zero TypeScript errors
- `ls frontend/src/app/(authenticated)/chat/page.tsx` — chat page exists in route group
- `ls frontend/src/app/(authenticated)/admin/layout.tsx` — admin layout moved
- `ls frontend/src/app/login/page.tsx` — login page NOT moved (stays public)
- NavRail component has role-gated admin check using session.realmRoles
</verification>

<success_criteria>
- 64px dark nav rail visible on all authenticated pages with Chat, Workflows, Skills, Admin (gated), Settings, Profile items
- Active item has blue left border indicator
- Admin visible only to admin/developer/it-admin roles
- Avatar dropdown contains Profile link and SignOut button
- Mobile tab bar with 5 items replaces nav rail below md breakpoint
- /login page has no nav rail
- All existing pages still accessible at their original URLs
</success_criteria>

<output>
After completion, create `.planning/phases/16-navigation-user-experience/16-02-SUMMARY.md`
</output>
