---
phase: 03.1-memory-read-path-and-mcp-hot-registration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/agents/test_master_agent_memory.py
  - backend/tests/mcp/test_mcp_registry.py
  - backend/agents/master_agent.py
  - backend/api/routes/mcp_servers.py
autonomous: true
requirements:
  - MEMO-02
  - INTG-01

must_haves:
  truths:
    - "When load_recent_episodes() returns episodes, _load_memory_node injects them as a SystemMessage with '[Medium-term memory' prefix before the conversation history"
    - "When load_recent_episodes() raises, _load_memory_node degrades gracefully — no exception propagates to the caller, agent continues without episode context"
    - "After create_mcp_server() commits to DB, MCPToolRegistry.refresh() is called exactly once so new server tools are immediately callable without a backend restart"
    - "Full test suite (pytest tests/ -q) passes with 0 failures after all fixes"
  artifacts:
    - path: "backend/tests/agents/test_master_agent_memory.py"
      provides: "Failing tests for episode injection (RED state)"
      contains: "test_load_memory_node_injects_episodes_as_system_message"
    - path: "backend/tests/mcp/test_mcp_registry.py"
      provides: "Failing test for MCP hot-registration (RED state)"
      contains: "test_create_mcp_server_calls_refresh_after_commit"
    - path: "backend/agents/master_agent.py"
      provides: "Episode injection wired into _load_memory_node"
      contains: "load_recent_episodes"
    - path: "backend/api/routes/mcp_servers.py"
      provides: "MCPToolRegistry.refresh() called after session.commit()"
      contains: "MCPToolRegistry.refresh"
  key_links:
    - from: "backend/agents/master_agent.py (module top)"
      to: "memory.medium_term.load_recent_episodes"
      via: "top-level import (not lazy) — required for patch('agents.master_agent.load_recent_episodes') to work"
      pattern: "from memory\\.medium_term import load_recent_episodes"
    - from: "backend/api/routes/mcp_servers.py create_mcp_server()"
      to: "mcp.registry.MCPToolRegistry.refresh()"
      via: "lazy import inside function body, called after await session.commit()"
      pattern: "await MCPToolRegistry\\.refresh\\(\\)"
---

<objective>
Close the two integration gaps found in the v1.0 milestone audit using TDD:
- MEMO-02: Wire medium-term episode summaries into `_load_memory_node` so old conversations surface in future sessions
- INTG-01: Call `MCPToolRegistry.refresh()` after `create_mcp_server()` so new MCP server tools are callable immediately

Both fixes are surgical wiring of existing, fully-tested infrastructure. Zero new dependencies, zero migrations.

Purpose: Both the write side (Celery episode summarization, MCP server DB registration) and the read side logic (load_recent_episodes, MCPToolRegistry.refresh) are fully implemented. The only gap is the connection between them. This plan closes that connection with tests first.

Output: 3 new test functions (RED → GREEN), 2 production file patches (~15 lines total), full suite green.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-memory-read-path-and-mcp-hot-registration/03.1-RESEARCH.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for episode injection and MCP hot-registration</name>
  <files>
    backend/tests/agents/test_master_agent_memory.py
    backend/tests/mcp/test_mcp_registry.py
  </files>
  <action>
    Write 3 failing tests (RED state) using existing AsyncMock patterns from the test files.

    In backend/tests/agents/test_master_agent_memory.py — add 2 tests:
    - test_load_memory_node_injects_episodes_as_system_message: patches load_recent_episodes to return a mock episode, sets state["messages"]=[] (skip_short_term=False path) so history is returned, asserts result["messages"] contains a SystemMessage with "[Medium-term memory" prefix
    - test_load_memory_node_gracefully_handles_episode_failure: patches load_recent_episodes to raise RuntimeError, asserts no exception propagates and result contains no "[Medium-term memory" SystemMessage

    In backend/tests/mcp/test_mcp_registry.py — add 1 test:
    - test_create_mcp_server_calls_refresh_after_commit: calls create_mcp_server() directly with mocked session and admin user, patches mcp.registry.MCPToolRegistry.refresh, asserts refresh was called once

    All 3 tests MUST FAIL in RED state (implementation not yet written). See full details in <feature> block below.
  </action>
  <feature>
    <name>Medium-term episode injection + MCP hot-registration</name>
    <files>
      backend/tests/agents/test_master_agent_memory.py
      backend/tests/mcp/test_mcp_registry.py
    </files>
    <behavior>
      Test A — episode injection happy path:
        load_recent_episodes returns [episode(summary="User discussed backend.")] → result messages must include a SystemMessage containing "[Medium-term memory"

      Test B — episode injection graceful failure:
        load_recent_episodes raises RuntimeError → _load_memory_node must NOT propagate the exception (no pytest.raises needed; just call and assert no raise)

      Test C — MCP hot-registration:
        create_mcp_server() route is invoked → MCPToolRegistry.refresh must be called exactly once after session.commit()
    </behavior>
    <implementation>
      Add 2 tests to existing backend/tests/agents/test_master_agent_memory.py:

      test_load_memory_node_injects_episodes_as_system_message:
        - Mirror the exact mock pattern of test_load_memory_node_injects_facts_as_system_message (same patches: search_facts, BGE_M3Provider, load_recent_turns, async_session, current_user_ctx, current_conversation_id_ctx)
        - Add patch("agents.master_agent.load_recent_episodes", new_callable=AsyncMock) → returns [mock_episode]
        - mock_episode.summary = "User discussed backend architecture preferences."
        - mock_search.return_value = [] (no long-term facts, isolate episode behavior)
        - state = {"messages": [HumanMessage(content="Tell me about our previous conversations")], "loaded_facts": [], "delivery_targets": ["WEB_CHAT"]}
        - Assert: any(isinstance(m, SystemMessage) and "[Medium-term memory" in m.content for m in result.get("messages", []))
        - NOTE: result["messages"] from _load_memory_node when short-term is already in state (skip_short_term=True) only returns loaded_facts, not messages. To see the SystemMessage, use a state WITH messages so skip_short_term=True and assert the SystemMessage was inserted at position 0 of the history variable — but that variable isn't returned directly. WORKAROUND: Set state["messages"] = [] so skip_short_term=False and the full history (with SystemMessage) is returned. This is consistent with how test_load_memory_node_injects_facts_as_system_message works — it provides state["messages"] = [HumanMessage(...)] which takes the skip_short_term=True path. CHECK the existing test to confirm what state path it tests and match accordingly.
        - CORRECT APPROACH: Read _load_memory_node carefully. When state["messages"] is non-empty, skip_short_term=True and the return is {"loaded_facts": loaded_facts} — messages are NOT returned (they were already in state). So to test that the SystemMessage was injected into the history, use state["messages"] = [] so history is built from DB turns (all mocked to return []), and episodes are inserted into history, and {"messages": history, "loaded_facts": loaded_facts} is returned. Set mock_turns.return_value = [] so history starts empty, episodes are inserted → result["messages"] will be [SystemMessage(...)].

      test_load_memory_node_gracefully_handles_episode_failure:
        - Same patches as above
        - load_recent_episodes raises RuntimeError("DB connection lost")
        - Assert: result is a dict (not an exception), and "[Medium-term memory" NOT in any message content

      Add 1 test to existing backend/tests/mcp/test_mcp_registry.py:

      test_create_mcp_server_calls_refresh_after_commit:
        - Import: from api.routes.mcp_servers import create_mcp_server, McpServerCreate
        - Use direct function call (not TestClient) with mocked session and mocked user
        - Mock the entire MCPToolRegistry class: patch("api.routes.mcp_servers.MCPToolRegistry") — NOTE: this requires adding the import INSIDE the route first (task 2), so for Wave 0 the test will fail with ImportError or AttributeError on MCPToolRegistry. That is the RED state — expected and correct.
        - Actually, since the import is lazy (inside the function), patch it as: patch("mcp.registry.MCPToolRegistry") won't work for lazy imports. Instead the test should patch the module reference that will exist after implementation. For Wave 0 RED state, the test should fail because MCPToolRegistry.refresh() is not called. Write the test as it should work after implementation:
          * body = McpServerCreate(name="test-crm", url="http://mcp-crm:8001", auth_token=None)
          * Create a mock admin user: UserContext(user_id=uuid.uuid4(), email="admin@blitz.local", username="admin", roles=["it-admin"], groups=[])
          * Mock session: AsyncMock with commit() and refresh() (refresh sets server.id, server.name, server.url, server.is_active)
          * patch("api.routes.mcp_servers.MCPToolRegistry") as mock_registry — this will work after implementation adds the import at function scope
          * mock_registry.refresh = AsyncMock()
          * await create_mcp_server(body=body, user=admin_user, session=mock_session)
          * assert mock_registry.refresh.call_count == 1
        - This test WILL fail in Wave 0 because MCPToolRegistry is not yet imported in mcp_servers.py (NameError). That is correct RED state.

      Run after Wave 0:
        cd backend && PYTHONPATH=. .venv/bin/pytest tests/agents/test_master_agent_memory.py -k episode -x
        cd backend && PYTHONPATH=. .venv/bin/pytest tests/mcp/test_mcp_registry.py -k hot_registration -x
      Both must FAIL (RED).
    </implementation>
  </feature>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/agents/test_master_agent_memory.py -k episode -x 2>&1 | tail -5 && PYTHONPATH=. .venv/bin/pytest tests/mcp/test_mcp_registry.py -k hot_registration -x 2>&1 | tail -5</automated>
    <manual>Both test runs must show FAILED (not ERROR from syntax). If they show PASSED something is wrong — the implementation must not exist yet.</manual>
    <sampling_rate>run immediately after writing tests, before implementation</sampling_rate>
  </verify>
  <done>
    - test_load_memory_node_injects_episodes_as_system_message exists in test_master_agent_memory.py and FAILS (AttributeError on load_recent_episodes or AssertionError on missing SystemMessage)
    - test_load_memory_node_gracefully_handles_episode_failure exists and FAILS
    - test_create_mcp_server_calls_refresh_after_commit exists in test_mcp_registry.py and FAILS (NameError on MCPToolRegistry or AssertionError on call count)
  </done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement episode injection in master_agent and hot-registration in mcp_servers</name>
  <files>
    backend/agents/master_agent.py
    backend/api/routes/mcp_servers.py
  </files>
  <action>
    MEMO-02 — backend/agents/master_agent.py:
    1. Add top-level import after `from memory.long_term import search_facts`:
       `from memory.medium_term import load_recent_episodes`
       (MUST be top-level, not lazy — lazy imports break patch("agents.master_agent.load_recent_episodes"), per STATE.md decision on call_mcp_tool)
    2. Inside _load_memory_node, after the long-term facts except block and before `if skip_short_term:`, add:
       ```python
       if user_id:
           try:
               async with async_session() as session:
                   episodes = await load_recent_episodes(session, user_id=user_id, n=3)
               if episodes:
                   episode_context = "\n".join(f"- {ep.summary}" for ep in episodes)
                   history.insert(0, SystemMessage(content="[Medium-term memory — summaries of past conversations:]\n" + episode_context))
                   logger.debug("medium_term_memory_loaded", episode_count=len(episodes), user_id=str(user_id))
           except Exception:
               logger.warning("medium_term_memory_load_failed", user_id=str(user_id))
       ```

    INTG-01 — backend/api/routes/mcp_servers.py:
    Inside create_mcp_server(), after `await session.refresh(server)` and before `logger.info(...)`, add:
    ```python
    from mcp.registry import MCPToolRegistry
    await MCPToolRegistry.refresh()
    ```
    Lazy import (inside function) avoids circular import. Patch target in tests: "mcp.registry.MCPToolRegistry".

    See full implementation details and pitfalls in <feature> blocks below.
  </action>
  <feature>
    <name>MEMO-02: Episode injection wiring</name>
    <files>backend/agents/master_agent.py</files>
    <behavior>
      _load_memory_node injects episode summaries as SystemMessage when load_recent_episodes() returns data.
      Gracefully degrades (try/except) when load_recent_episodes() raises.
    </behavior>
    <implementation>
      1. Add top-level import (line after `from memory.long_term import search_facts`):
         ```python
         from memory.medium_term import load_recent_episodes
         ```
         CRITICAL: Must be at module top level, NOT inside _load_memory_node. Lazy imports break patch("agents.master_agent.load_recent_episodes"). See STATE.md decision: "[Phase 03-04]: call_mcp_tool imported at top level in project_agent.py (not lazily)".

      2. Inside `_load_memory_node`, add the episode injection block AFTER the long-term facts block (after the closing `except Exception:` of the long-term facts block, before the `if skip_short_term:` check):

         ```python
         # Medium-term memory: recent episode summaries (cross-session context).
         # Loaded unconditionally on user_id — not query-dependent like long-term facts.
         if user_id:
             try:
                 async with async_session() as session:
                     episodes = await load_recent_episodes(session, user_id=user_id, n=3)
                 if episodes:
                     episode_context = "\n".join(f"- {ep.summary}" for ep in episodes)
                     history.insert(
                         0,
                         SystemMessage(
                             content=(
                                 "[Medium-term memory — summaries of past conversations:]\n"
                                 + episode_context
                             )
                         ),
                     )
                     logger.debug(
                         "medium_term_memory_loaded",
                         episode_count=len(episodes),
                         user_id=str(user_id),
                     )
             except Exception:
                 # Graceful degradation: episode load failure must not block the agent
                 logger.warning("medium_term_memory_load_failed", user_id=str(user_id))
         ```

         ORDERING NOTE: Long-term facts are inserted at history[0] in the block above. Episodes are inserted at history[0] AFTER that block — this pushes facts to history[1]. Final ordering: episodes (0), facts (1), conversation turns (2+). This is correct: broader context (episodes) before specific facts.

         POSITION: Insert this block between the long-term facts block's `except Exception:` / `logger.warning("long_term_memory_load_failed")` line and the `if skip_short_term:` line.
    </implementation>
  </feature>
  <feature>
    <name>INTG-01: MCP hot-registration wiring</name>
    <files>backend/api/routes/mcp_servers.py</files>
    <behavior>
      create_mcp_server() calls MCPToolRegistry.refresh() after session.commit() so the new server's tools are immediately available in the in-process registry.
    </behavior>
    <implementation>
      In `create_mcp_server()`, after `await session.refresh(server)`, add:

      ```python
      # Hot-register: make new server's tools immediately callable without restart.
      # refresh() is idempotent — re-queries all active servers from DB.
      from mcp.registry import MCPToolRegistry
      await MCPToolRegistry.refresh()
      ```

      Place this BEFORE the `logger.info("mcp_server_registered", ...)` line so the log entry marks the operation complete (server registered AND tools hot-loaded).

      NOTE: Lazy import inside the function body (not at module top) is intentional here — avoids circular import between api.routes.mcp_servers and mcp.registry. The test patches "api.routes.mcp_servers.MCPToolRegistry" which works for function-scoped imports when the patch is applied before the function is called. Actually: for a lazy import inside a function, patch("api.routes.mcp_servers.MCPToolRegistry") won't intercept the import that happens inside the function call. The correct patch target for a lazy import is the class in its definition module: patch("mcp.registry.MCPToolRegistry"). Re-examine test_create_mcp_server_calls_refresh_after_commit (Task 1) and update the patch target if needed to use "mcp.registry.MCPToolRegistry" — after which mock_registry_class.refresh = AsyncMock() and assert mock_registry_class.refresh.called_once().
    </implementation>
  </feature>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/agents/test_master_agent_memory.py -k episode -x -v && PYTHONPATH=. .venv/bin/pytest tests/mcp/test_mcp_registry.py -k hot_registration -x -v</automated>
    <manual>Both targeted test runs must show PASSED (GREEN). Then run full suite.</manual>
    <sampling_rate>run after each file edit, before next file</sampling_rate>
  </verify>
  <done>
    - test_load_memory_node_injects_episodes_as_system_message: PASSED
    - test_load_memory_node_gracefully_handles_episode_failure: PASSED
    - test_create_mcp_server_calls_refresh_after_commit: PASSED
    - from memory.medium_term import load_recent_episodes appears at top of master_agent.py
    - await MCPToolRegistry.refresh() appears inside create_mcp_server() after session.commit()
  </done>
</task>

<task type="auto">
  <name>Task 3: Full suite green — all 177+ tests pass with 0 failures</name>
  <files></files>
  <action>
    Run the full test suite and confirm 0 failures. This is the integration gate for the phase.

    ```bash
    cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q
    ```

    Expected output: `177+ passed, 0 failed` (or more if new tests brought the count above 177).

    If any tests fail:
    1. Identify failing test names
    2. Check if failure is in newly added tests (fix the test or implementation per RED→GREEN)
    3. Check if failure is a regression in existing tests (check for import side-effects from new top-level import)
    4. Common regression risk: adding `from memory.medium_term import load_recent_episodes` at module top may cause ImportError if any existing test imports master_agent without mocking memory.medium_term. Inspect the failure traceback.
    5. Fix the root cause; do NOT skip or xfail tests unless they were already marked as such.

    After suite is green, update VALIDATION.md task status fields and nyquist_compliant to true.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q 2>&1 | tail -5</automated>
    <manual>Output must show "passed" with 0 failures. Count must be >= 180 (177 existing + 3 new tests).</manual>
    <sampling_rate>run after Tasks 1 and 2 are both complete</sampling_rate>
  </verify>
  <done>
    Full suite: N passed, 0 failed, 0 errors. N >= 180.
    VALIDATION.md updated: nyquist_compliant: true, all task statuses marked green.
  </done>
</task>

</tasks>

<verification>
Phase 3.1 is complete when all three success criteria from ROADMAP.md hold:

1. **MEMO-02:** `test_load_memory_node_injects_episodes_as_system_message` PASSES — verifies that after Celery summarizes a conversation, the next session injects those summaries into agent context as `[Medium-term memory...]` SystemMessage.

2. **INTG-01:** `test_create_mcp_server_calls_refresh_after_commit` PASSES — verifies that newly registered MCP servers are immediately available without backend restart.

3. **Full suite:** `pytest tests/ -q` shows 0 failures across all 180+ tests.

Automated gate commands:
```bash
cd /home/tungmv/Projects/hox-agentos/backend
PYTHONPATH=. .venv/bin/pytest tests/agents/test_master_agent_memory.py -k episode -v
PYTHONPATH=. .venv/bin/pytest tests/mcp/test_mcp_registry.py -k hot_registration -v
PYTHONPATH=. .venv/bin/pytest tests/ -q
```
</verification>

<success_criteria>
- `from memory.medium_term import load_recent_episodes` is a top-level import in `backend/agents/master_agent.py`
- `_load_memory_node` has an episode injection block after the long-term facts block with graceful try/except
- `create_mcp_server()` in `backend/api/routes/mcp_servers.py` calls `await MCPToolRegistry.refresh()` after `await session.commit()`
- 3 new tests added: 2 in `test_master_agent_memory.py`, 1 in `test_mcp_registry.py`
- `pytest tests/ -q` reports 0 failures with >= 180 tests collected
- REQUIREMENTS.md MEMO-02 and INTG-01 can be marked as Done
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-memory-read-path-and-mcp-hot-registration/03.1-01-SUMMARY.md` following the standard summary template at `/home/tungmv/.claude/get-shit-done/templates/summary.md`.

Update `.planning/REQUIREMENTS.md`: change `- [ ] **MEMO-02**` to `- [x] **MEMO-02**` and `- [ ] **INTG-01**` to `- [x] **INTG-01**`.

Update `.planning/ROADMAP.md`: mark phase 3.1 as complete in the Progress table.
</output>
