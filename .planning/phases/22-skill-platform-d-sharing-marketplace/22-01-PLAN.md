---
phase: 22
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/025_skill_is_promoted.py
  - backend/core/models/skill_definition.py
  - backend/core/schemas/registry.py
  - backend/api/routes/admin_skills.py
  - backend/api/routes/user_skills.py
  - backend/api/routes/admin_skill_sharing.py
  - backend/main.py
  - backend/tests/api/test_admin_skill_promote.py
  - backend/tests/api/test_user_skill_export.py
  - backend/tests/api/test_skill_sharing.py
  - backend/tests/api/test_user_skills.py
autonomous: true
requirements:
  - SKMKT-01
  - SKMKT-02
  - SKMKT-03
must_haves:
  truths:
    - "Admin can toggle is_promoted on a skill via PATCH /api/admin/skills/{id}/promote and GET /api/skills returns is_promoted in each item"
    - "GET /api/skills includes is_shared: bool on each item — skills shared with the user have is_shared=true alongside the main catalog results"
    - "Any authenticated user can download GET /api/skills/{id}/export and receive a ZIP file"
    - "Admin can POST /api/admin/skills/{id}/share to grant a user access, GET /api/admin/skills/{id}/shares to list, DELETE to revoke"
  artifacts:
    - path: backend/alembic/versions/025_skill_is_promoted.py
      provides: Alembic migration adding is_promoted column
    - path: backend/core/schemas/registry.py
      provides: SkillListItem with is_promoted + is_shared, SkillShareRequest, SkillShareEntry schemas
    - path: backend/api/routes/user_skills.py
      provides: GET /api/skills with is_shared JOIN + promoted filter + export endpoint
    - path: backend/api/routes/admin_skill_sharing.py
      provides: Admin POST/DELETE/GET sharing endpoints
  key_links:
    - from: backend/api/routes/user_skills.py
      to: backend/core/models/user_artifact_permission.py
      via: LEFT JOIN on artifact_type=skill and user_id from JWT
      pattern: "UserArtifactPermission.*artifact_type.*skill"
---

<objective>
Add backend foundations for Phase 22: is_promoted column with toggle endpoint, is_shared JOIN on the main skills list, user-facing export endpoint, and admin skill-sharing endpoints (POST/DELETE/GET).

Purpose: All three Phase 22 requirements (SKMKT-01, SKMKT-02, SKMKT-03) depend on these backend endpoints. Plans 22-02 and 22-03 build UI on top of these.

Output: Migration 025, updated schemas, 4 new/updated routes, tests passing.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-skill-platform-d-sharing-marketplace/22-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 025 + is_promoted ORM column + schema fields</name>
  <files>
    backend/alembic/versions/025_skill_is_promoted.py
    backend/core/models/skill_definition.py
    backend/core/schemas/registry.py
  </files>
  <behavior>
    - Migration file has revision="025", down_revision="024", upgrades with op.add_column("skill_definitions", Boolean NOT NULL server_default false), downgrades with op.drop_column
    - SkillDefinition ORM model has is_promoted: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("false"))
    - SkillListItem schema has is_promoted: bool = False and is_shared: bool = False
    - SkillDefinitionResponse schema has is_promoted: bool field
    - SkillShareRequest schema: user_id: uuid.UUID
    - SkillShareEntry schema: user_id: uuid.UUID, granted_at: datetime, model_config ConfigDict(from_attributes=True)
    - All existing tests still pass (additive changes with defaults)
  </behavior>
  <action>
    Step 1: Create backend/alembic/versions/025_skill_is_promoted.py with revision="025", down_revision="024". In upgrade(): op.add_column("skill_definitions", sa.Column("is_promoted", sa.Boolean, nullable=False, server_default=sa.text("false"))). In downgrade(): op.drop_column("skill_definitions", "is_promoted").

    Step 2: In backend/core/models/skill_definition.py, add after the usage_count column:
      is_promoted: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("false"))

    Step 3: In backend/core/schemas/registry.py, add to SkillListItem: is_promoted: bool = False and is_shared: bool = False. Add to SkillDefinitionResponse: is_promoted: bool. Add at the end of the file:
      class SkillShareRequest(BaseModel):
          user_id: uuid.UUID

      class SkillShareEntry(BaseModel):
          user_id: uuid.UUID
          granted_at: datetime
          model_config = ConfigDict(from_attributes=True)

    Step 4: Run full test suite to confirm nothing broken:
      cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q

    Step 5: Commit:
      git add backend/alembic/versions/025_skill_is_promoted.py backend/core/models/skill_definition.py backend/core/schemas/registry.py
      git commit -m "feat(22-01): migration 025 — is_promoted column, schemas for sharing"
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q</automated>
  </verify>
  <done>Migration file exists, SkillDefinition.is_promoted column present, SkillListItem has is_promoted and is_shared fields, sharing schemas defined, all existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH /promote endpoint + GET /api/skills with is_shared + promoted filter</name>
  <files>
    backend/api/routes/admin_skills.py
    backend/api/routes/user_skills.py
    backend/tests/api/test_admin_skill_promote.py
    backend/tests/api/test_user_skills.py
  </files>
  <behavior>
    - PATCH /api/admin/skills/{id}/promote toggles is_promoted (True→False→True); requires registry:manage permission; returns SkillDefinitionResponse with is_promoted flipped; returns 404 for unknown skill
    - GET /api/skills LEFT JOINs UserArtifactPermission WHERE artifact_type='skill' AND user_id=current_user AND allowed=True AND status='active' — each SkillListItem has is_shared=True if a matching permission row exists
    - GET /api/skills?promoted=true filters to only is_promoted==True skills
    - test_admin_skill_promote.py: test_promote_skill (200, is_promoted True), test_unpromote_skill (200, is_promoted False after second call), test_promote_not_found (404)
    - test_user_skills.py: existing tests pass; new test_list_includes_is_shared confirms is_shared field present on all items
  </behavior>
  <action>
    Step 1: Write tests first (TDD RED phase).

    Create backend/tests/api/test_admin_skill_promote.py with sqlite in-memory fixture, admin UserContext (roles=["admin"]), seeded SkillDefinition(is_promoted=False). Tests:
    - test_promote_skill: PATCH /api/admin/skills/{id}/promote → 200, data["is_promoted"] == True
    - test_unpromote_skill: PATCH twice → 200, data["is_promoted"] == False
    - test_promote_not_found: PATCH with uuid4() → 404

    Run to confirm FAIL:
      cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_admin_skill_promote.py -v

    Step 2: Add promote endpoint to backend/api/routes/admin_skills.py. Insert BEFORE the /{skill_id} UUID routes (to avoid route conflicts). The endpoint must be at path "/{skill_id}/promote":

      @router.patch("/{skill_id}/promote")
      async def toggle_skill_promoted(
          skill_id: UUID,
          user: UserContext = Depends(_require_registry_manager),
          session: AsyncSession = Depends(get_db),
      ) -> SkillDefinitionResponse:
          result = await session.execute(select(SkillDefinition).where(SkillDefinition.id == skill_id))
          skill = result.scalar_one_or_none()
          if skill is None:
              raise HTTPException(status_code=404, detail="Skill not found")
          skill.is_promoted = not skill.is_promoted
          await session.commit()
          await session.refresh(skill)
          logger.info("admin_skill_promoted_toggled", skill_id=str(skill_id), is_promoted=skill.is_promoted, user_id=str(user["user_id"]))
          return SkillDefinitionResponse.model_validate(skill)

    Step 3: Update GET /api/skills in backend/api/routes/user_skills.py to:
    (a) Add promoted: bool | None = Query(None) parameter
    (b) LEFT JOIN UserArtifactPermission on artifact_type='skill', artifact_id=SkillDefinition.id, user_id=current_user.user_id, allowed=True, status='active' — select both SkillDefinition and a boolean indicating whether a permission row exists
    (c) Set is_shared on each SkillListItem based on whether permission row was found
    (d) Apply promoted filter: if promoted is not None: stmt = stmt.where(SkillDefinition.is_promoted == promoted)

    The SQLAlchemy query pattern using outerjoin:
      from sqlalchemy import outerjoin, exists
      from core.models.user_artifact_permission import UserArtifactPermission

      # Use a correlated exists subquery for is_shared:
      shared_subq = (
          select(UserArtifactPermission.id)
          .where(
              UserArtifactPermission.artifact_type == "skill",
              UserArtifactPermission.artifact_id == SkillDefinition.id,
              UserArtifactPermission.user_id == user["user_id"],
              UserArtifactPermission.allowed == True,
              UserArtifactPermission.status == "active",
          )
          .exists()
          .label("is_shared")
      )
      stmt = select(SkillDefinition, shared_subq).where(SkillDefinition.status == "active", SkillDefinition.is_active == True)
      # apply filters, sort...
      result = await session.execute(stmt)
      rows = result.all()
      items = []
      for skill, is_shared in rows:
          item = SkillListItem.model_validate(skill)
          item.is_shared = bool(is_shared)
          items.append(item)
      return items

    Step 4: Run all skill tests:
      cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_admin_skill_promote.py tests/api/test_user_skills.py -v

    Step 5: Run full suite:
      cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q

    Step 6: Commit:
      git add backend/api/routes/admin_skills.py backend/api/routes/user_skills.py backend/tests/api/test_admin_skill_promote.py backend/tests/api/test_user_skills.py
      git commit -m "feat(22-01): PATCH promote endpoint + GET /api/skills with is_shared + promoted filter"
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_admin_skill_promote.py tests/api/test_user_skills.py -v</automated>
  </verify>
  <done>PATCH /promote toggles is_promoted (3 tests pass). GET /api/skills returns is_shared on all items and filters by promoted=true.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GET /api/skills/{id}/export + admin sharing endpoints (POST/DELETE/GET)</name>
  <files>
    backend/api/routes/user_skills.py
    backend/api/routes/admin_skill_sharing.py
    backend/main.py
    backend/tests/api/test_user_skill_export.py
    backend/tests/api/test_skill_sharing.py
  </files>
  <behavior>
    - GET /api/skills/{id}/export: authenticated user (not admin-only); fetches active skill by UUID; calls build_skill_zip(skill); returns StreamingResponse with media_type="application/zip" and Content-Disposition header; returns 404 for unknown/inactive skills
    - POST /api/admin/skills/{id}/share: requires registry:manage; creates UserArtifactPermission(artifact_type="skill", artifact_id=skill_id, user_id=body.user_id, allowed=True, status="active"); returns 201 + SkillShareEntry; returns 409 on duplicate (IntegrityError)
    - DELETE /api/admin/skills/{id}/share/{user_id}: requires registry:manage; deletes permission row; returns 204; returns 404 if not found
    - GET /api/admin/skills/{id}/shares: requires registry:manage; returns list[SkillShareEntry] for all active shares of that skill
    - Export route declared BEFORE /{skill_name}/run to avoid routing conflicts
    - admin_skill_sharing router registered in main.py BEFORE admin_skills.router
  </behavior>
  <action>
    Step 1: Write tests (TDD RED).

    Create backend/tests/api/test_user_skill_export.py: sqlite in-memory fixture, employee UserContext (roles=["employee"]), seeded active SkillDefinition. Tests:
    - test_user_export_skill_returns_zip: GET /api/skills/{id}/export → 200, content-type application/zip, "export-skill" in content-disposition
    - test_user_export_skill_not_found: GET /api/skills/{uuid4()}/export → 404

    Create backend/tests/api/test_skill_sharing.py: sqlite in-memory fixture, admin UserContext. Tests:
    - test_share_skill: POST /api/admin/skills/{id}/share {"user_id": str(_TARGET_USER_ID)} → 201, data["user_id"] == str(_TARGET_USER_ID)
    - test_share_skill_duplicate_returns_409: POST twice → 409
    - test_list_shares: POST then GET /api/admin/skills/{id}/shares → 200, list len 1
    - test_revoke_share: POST then DELETE /api/admin/skills/{id}/share/{user_id} → 204; subsequent GET returns []
    - test_revoke_nonexistent_share_returns_404: DELETE non-existent → 404

    Run to confirm FAIL:
      cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_user_skill_export.py tests/api/test_skill_sharing.py -v

    Step 2: Add export route to backend/api/routes/user_skills.py. Add before the /{skill_name}/run route:

      from fastapi.responses import StreamingResponse
      from skill_export.exporter import build_skill_zip

      @router.get("/{skill_id}/export")
      async def export_user_skill(
          skill_id: UUID,
          user: UserContext = Depends(_require_chat),
          session: AsyncSession = Depends(get_user_db),
      ) -> StreamingResponse:
          result = await session.execute(
              select(SkillDefinition).where(
                  SkillDefinition.id == skill_id,
                  SkillDefinition.status == "active",
                  SkillDefinition.is_active == True,
              )
          )
          skill = result.scalar_one_or_none()
          if skill is None:
              raise HTTPException(status_code=404, detail="Skill not found or not active")
          zip_bytes = build_skill_zip(skill)
          filename = f"{skill.name}-{skill.version}.zip"
          logger.info("user_skill_exported", skill_id=str(skill_id), skill_name=skill.name, user_id=str(user["user_id"]))
          return StreamingResponse(
              zip_bytes,
              media_type="application/zip",
              headers={"Content-Disposition": f'attachment; filename="{filename}"'},
          )

    Step 3: Create backend/api/routes/admin_skill_sharing.py:
      - router = APIRouter(prefix="/api/admin/skills", tags=["admin-skills-sharing"])
      - POST /{skill_id}/share: creates UserArtifactPermission, catches IntegrityError → 409
      - DELETE /{skill_id}/share/{target_user_id}: finds and deletes permission, 404 if not found
      - GET /{skill_id}/shares: returns list[SkillShareEntry] for all active shares
      - All three endpoints use Depends(require_registry_manager)
      - Import SkillShareRequest, SkillShareEntry from core.schemas.registry

    Step 4: Register admin_skill_sharing router in backend/main.py. Import it and include it BEFORE admin_skills.router in the include_router calls to avoid route shadowing:
      from api.routes.admin_skill_sharing import router as admin_skill_sharing_router
      app.include_router(admin_skill_sharing_router)  # before admin_skills.router

    Step 5: Run all tests:
      cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_user_skill_export.py tests/api/test_skill_sharing.py -v

    Step 6: Full suite:
      cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q

    Step 7: Commit:
      git add backend/api/routes/user_skills.py backend/api/routes/admin_skill_sharing.py backend/main.py backend/tests/api/test_user_skill_export.py backend/tests/api/test_skill_sharing.py
      git commit -m "feat(22-01): GET /api/skills/{id}/export + admin sharing endpoints"
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_user_skill_export.py tests/api/test_skill_sharing.py tests/ -q</automated>
  </verify>
  <done>Export endpoint returns ZIP for active skill, 404 for unknown. Admin sharing: POST 201, duplicate 409, GET lists shares, DELETE 204, DELETE nonexistent 404. All tests pass.</done>
</task>

</tasks>

<verification>
All backend endpoints for Phase 22 implemented and tested:
- cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q
- .venv/bin/alembic heads (should show 025)
- Migration 025 file exists at backend/alembic/versions/025_skill_is_promoted.py
- SkillListItem has is_promoted and is_shared fields
- admin_skill_sharing router registered in main.py before admin_skills
</verification>

<success_criteria>
- All existing tests still pass (no regression)
- PATCH /api/admin/skills/{id}/promote toggles is_promoted (3 tests)
- GET /api/skills returns is_shared on every item, filters by promoted=true
- GET /api/skills/{id}/export returns ZIP for active skills, 404 for unknown
- POST/DELETE/GET admin sharing endpoints work (5 tests)
</success_criteria>

<output>
After completion, create .planning/phases/22-skill-platform-d-sharing-marketplace/22-01-SUMMARY.md
</output>
