---
phase: 22
plan: "02"
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - frontend/src/lib/admin-types.ts
  - frontend/src/hooks/use-admin-artifacts.ts
  - frontend/src/app/(authenticated)/admin/skills/page.tsx
autonomous: true
requirements:
  - SKMKT-01
  - SKMKT-03
must_haves:
  truths:
    - "Admin can click the ⋮ card action menu on any skill card and see a 'Promote' (or 'Unpromote') item — clicking it toggles is_promoted and the card updates"
    - "Admin can click 'Share with user...' from the ⋮ card action menu, type a name/email in the dialog search field, select a user from the dropdown, and grant access"
    - "The sharing dialog lists current shares with a Revoke button per user — admin can remove access inline"
  artifacts:
    - path: frontend/src/lib/admin-types.ts
      provides: SkillDefinition.isPromoted field, SkillShareEntry type
    - path: frontend/src/hooks/use-admin-artifacts.ts
      provides: isPromoted mapping from snake_case API response
    - path: frontend/src/app/(authenticated)/admin/skills/page.tsx
      provides: Promote/Unpromote in ⋮ action menu; Share dialog with user search + current shares list
  key_links:
    - from: frontend/src/app/(authenticated)/admin/skills/page.tsx
      to: PATCH /api/admin/skills/{id}/promote
      via: handlePromote() called from ⋮ action menu onClick
    - from: frontend/src/app/(authenticated)/admin/skills/page.tsx
      to: GET /api/admin/users
      via: user search field in share dialog, debounced query, dropdown results
---

<objective>
Add admin UI for the two new skill management actions: promoting skills via the card action menu, and sharing skills with specific users via a search dialog opened from the card action menu.

Purpose: SKMKT-01 (promoted curated section) requires admin to control which skills are promoted. SKMKT-03 (sharing) requires admin to grant per-user access.

Output: Promote action in ⋮ menu, share dialog with username/email search and current shares list.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-skill-platform-d-sharing-marketplace/22-CONTEXT.md
@.planning/phases/22-skill-platform-d-sharing-marketplace/22-01-SUMMARY.md
</context>

<interfaces>
<!-- From 22-01 backend endpoints (executor uses these directly) -->

PATCH /api/admin/skills/{id}/promote
  → 200 SkillDefinitionResponse with is_promoted toggled

POST /api/admin/skills/{id}/share
  body: { user_id: string }
  → 201 SkillShareEntry { user_id: string, granted_at: string }
  → 409 if already shared

DELETE /api/admin/skills/{id}/share/{user_id}
  → 204

GET /api/admin/skills/{id}/shares
  → SkillShareEntry[]

GET /api/admin/users?q=searchterm
  → existing endpoint, returns user list with id, email, username fields
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types — isPromoted + SkillShareEntry</name>
  <files>
    frontend/src/lib/admin-types.ts
    frontend/src/hooks/use-admin-artifacts.ts
  </files>
  <action>
    Step 1: In frontend/src/lib/admin-types.ts, find the SkillDefinition interface. Add isPromoted: boolean after usageCount:
      usageCount: number;
      isPromoted: boolean;

    Add SkillShareEntry interface (in the skills section):
      export interface SkillShareEntry {
        user_id: string;
        granted_at: string;
      }

    Step 2: In frontend/src/hooks/use-admin-artifacts.ts, find where skill items are mapped from API response (look for usageCount or usage_count). Add alongside it:
      isPromoted: Boolean(item.is_promoted ?? item.isPromoted ?? false),

    Step 3: Type-check:
      cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit

    Step 4: Commit:
      git add frontend/src/lib/admin-types.ts frontend/src/hooks/use-admin-artifacts.ts
      git commit -m "feat(22-02): add isPromoted field and SkillShareEntry type"
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>SkillDefinition.isPromoted: boolean present, SkillShareEntry exported from admin-types.ts, hook maps is_promoted correctly, no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Promote action in ⋮ card menu + Share dialog with user search</name>
  <files>
    frontend/src/app/(authenticated)/admin/skills/page.tsx
  </files>
  <action>
    This task implements two admin actions. Both are triggered from the existing ⋮ card action menu (the same menu used for edit/delete/clone/export). Do NOT put these in the skill detail drawer — the locked design decision places them in the card action menu.

    Step 1: Add state variables at the top of AdminSkillsPage component (alongside existing useState calls):
      const [shareDialogSkill, setShareDialogSkill] = useState<SkillDefinition | null>(null);
      const [userSearchQuery, setUserSearchQuery] = useState("");
      const [userSearchResults, setUserSearchResults] = useState<Array<{id: string; email: string; username: string}>>([]);
      const [userSearchLoading, setUserSearchLoading] = useState(false);
      const [shares, setShares] = useState<SkillShareEntry[]>([]);
      const [sharesLoading, setSharesLoading] = useState(false);
      const [shareError, setShareError] = useState<string | null>(null);

    Import SkillShareEntry:
      import type { SkillDefinition, SkillDefinitionCreate, SkillShareEntry } from "@/lib/admin-types";

    Step 2: Add handlePromote function (alongside existing handleExport):
      const handlePromote = async (skill: SkillDefinition) => {
        try {
          const res = await fetch(`/api/admin/skills/${skill.id}/promote`, { method: "PATCH" });
          if (!res.ok) return;
          refetch(); // refresh list so badge updates
        } catch {
          // non-critical
        }
      };

    Step 3: Add loadShares and user search functions:
      const loadShares = async (skillId: string) => {
        setSharesLoading(true);
        setShareError(null);
        try {
          const res = await fetch(`/api/admin/skills/${skillId}/shares`);
          if (!res.ok) { setShareError("Failed to load shares"); return; }
          setShares((await res.json()) as SkillShareEntry[]);
        } catch { setShareError("Network error"); } finally { setSharesLoading(false); }
      };

      const searchUsers = async (q: string) => {
        if (!q.trim()) { setUserSearchResults([]); return; }
        setUserSearchLoading(true);
        try {
          const res = await fetch(`/api/admin/users?q=${encodeURIComponent(q)}`);
          if (!res.ok) return;
          const data = await res.json() as Array<{id: string; email: string; username: string}>;
          setUserSearchResults(data);
        } catch { /* non-fatal */ } finally { setUserSearchLoading(false); }
      };

    Step 4: Add "Promote/Unpromote" to the ⋮ card action menu items. Find where the action menu items array is defined for the skill card grid (look for the existing edit/delete/clone/export action items). Add a promote item:
      {
        label: skill.isPromoted ? "Unpromote" : "Promote",
        onClick: () => handlePromote(skill),
      }

    Also add a "Share with user..." item that opens the share dialog:
      {
        label: "Share with user...",
        onClick: () => {
          setShareDialogSkill(skill);
          setUserSearchQuery("");
          setUserSearchResults([]);
          void loadShares(skill.id);
        },
      }

    Step 5: Add a promoted badge to the card's renderExtra or badge area. In the renderExtra callback for the admin skill cards, add:
      {(item as SkillDefinition).isPromoted && (
        <span className="inline-flex items-center px-1.5 py-0.5 rounded text-xs bg-amber-100 text-amber-700 font-medium">
          Promoted
        </span>
      )}

    Step 6: Add the Share dialog modal. Add after the main card grid JSX (at the end of the component return, before closing tags):
      {shareDialogSkill && (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50" onClick={() => setShareDialogSkill(null)}>
          <div className="bg-white rounded-lg shadow-xl p-6 w-full max-w-md" onClick={(e) => e.stopPropagation()}>
            <h3 className="text-sm font-semibold text-gray-900 mb-4">
              Share "{shareDialogSkill.displayName ?? shareDialogSkill.name}" with user
            </h3>

            {/* User search field — queries GET /api/admin/users */}
            <div className="relative mb-3">
              <input
                type="text"
                value={userSearchQuery}
                onChange={(e) => {
                  setUserSearchQuery(e.target.value);
                  void searchUsers(e.target.value);
                }}
                placeholder="Search by name or email..."
                className="w-full text-sm border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-1 focus:ring-blue-500"
                autoFocus
              />
              {userSearchLoading && (
                <span className="absolute right-3 top-2.5 text-xs text-gray-400">Searching...</span>
              )}
              {userSearchResults.length > 0 && (
                <ul className="absolute z-10 w-full bg-white border border-gray-200 rounded shadow-lg mt-1 max-h-48 overflow-y-auto">
                  {userSearchResults.map((u) => (
                    <li key={u.id}>
                      <button
                        className="w-full text-left px-3 py-2 text-sm hover:bg-blue-50 flex flex-col"
                        onClick={async () => {
                          setUserSearchQuery("");
                          setUserSearchResults([]);
                          setShareError(null);
                          const res = await fetch(`/api/admin/skills/${shareDialogSkill.id}/share`, {
                            method: "POST",
                            headers: { "Content-Type": "application/json" },
                            body: JSON.stringify({ user_id: u.id }),
                          });
                          if (res.status === 201) {
                            const entry = (await res.json()) as SkillShareEntry;
                            setShares((prev) => [...prev, entry]);
                          } else if (res.status === 409) {
                            setShareError("Already shared with this user");
                          } else {
                            setShareError("Failed to share skill");
                          }
                        }}
                      >
                        <span className="font-medium">{u.username}</span>
                        <span className="text-xs text-gray-500">{u.email}</span>
                      </button>
                    </li>
                  ))}
                </ul>
              )}
            </div>

            {/* Current shares list */}
            <div className="mt-4">
              <p className="text-xs font-semibold text-gray-700 mb-2">Currently shared with</p>
              {sharesLoading && <p className="text-xs text-gray-400">Loading...</p>}
              {!sharesLoading && shares.length === 0 && (
                <p className="text-xs text-gray-400">Not shared with anyone yet.</p>
              )}
              {shares.map((share) => (
                <div key={share.user_id} className="flex items-center justify-between mb-1.5">
                  <span className="text-xs font-mono text-gray-600 truncate max-w-xs">{share.user_id}</span>
                  <button
                    onClick={async () => {
                      const res = await fetch(`/api/admin/skills/${shareDialogSkill.id}/share/${share.user_id}`, { method: "DELETE" });
                      if (res.ok || res.status === 204) {
                        setShares((prev) => prev.filter((s) => s.user_id !== share.user_id));
                      }
                    }}
                    className="ml-2 text-xs text-red-500 hover:text-red-700 shrink-0"
                  >
                    Revoke
                  </button>
                </div>
              ))}
            </div>

            {shareError && <p className="text-xs text-red-500 mt-2">{shareError}</p>}

            <div className="mt-4 flex justify-end">
              <button
                onClick={() => setShareDialogSkill(null)}
                className="px-4 py-2 text-sm text-gray-700 border border-gray-300 rounded hover:bg-gray-50"
              >
                Close
              </button>
            </div>
          </div>
        </div>
      )}

    Step 7: Full build:
      cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build

    Step 8: Commit:
      git add frontend/src/app/(authenticated)/admin/skills/page.tsx
      git commit -m "feat(22-02): Promote in card menu + Share dialog with user search"
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build</automated>
  </verify>
  <done>
    - Promote/Unpromote action appears in ⋮ card action menu for admin skill cards
    - Promoted skills show amber "Promoted" badge on their cards
    - "Share with user..." action opens a modal with username/email search field calling GET /api/admin/users
    - Selecting a user from dropdown calls POST /api/admin/skills/{id}/share and adds to the current shares list
    - Revoke button calls DELETE and removes the entry
    - pnpm run build passes with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
Admin skills page compiles with no errors. Promote and share actions are in the ⋮ card action menu, not in any detail drawer. Share dialog uses user search (not raw UUID input).

cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build
</verification>

<success_criteria>
- pnpm run build passes cleanly
- SkillDefinition.isPromoted: boolean in admin-types.ts
- Promote/Unpromote in ⋮ card action menu (handlePromote calls PATCH /api/admin/skills/{id}/promote)
- Share dialog opens from ⋮ menu "Share with user..." — has search field that queries GET /api/admin/users
- Share dialog shows current shares list with Revoke button per user
</success_criteria>

<output>
After completion, create .planning/phases/22-skill-platform-d-sharing-marketplace/22-02-SUMMARY.md
</output>
