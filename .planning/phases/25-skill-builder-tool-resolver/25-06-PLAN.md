---
phase: 25-skill-builder-tool-resolver
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/routes/admin_skills.py
  - backend/tests/api/test_admin_skills.py
autonomous: true
gap_closure: true
requirements:
  - "uat-gap: create_skill_default_status"

must_haves:
  truths:
    - "A skill saved via POST /api/admin/skills returns status='draft'"
    - "test_activate_skill still passes after the fix"
    - "A new test asserts POST /api/admin/skills returns status='draft'"
    - "test_bulk_status_update passes with untouched skill asserting status='draft'"
  artifacts:
    - path: "backend/api/routes/admin_skills.py"
      provides: "create_skill endpoint with status='draft'"
      contains: "status=\"draft\""
    - path: "backend/tests/api/test_admin_skills.py"
      provides: "test asserting new skill starts as draft"
      contains: "test_create_skill_defaults_to_draft"
  key_links:
    - from: "admin_skills.py create_skill"
      to: "RegistryEntryCreate"
      via: "status='draft' kwarg"
      pattern: "status=\"draft\""
---

<objective>
Close UAT gap: skills saved via POST /api/admin/skills start life as `draft`, not `active`.

Purpose: The entire tool-resolver promotion pipeline (draft → pending_activation → active) is broken because the create endpoint bypasses it by defaulting to `active`. Tests 4-7 in the UAT were all skipped due to this cascade.

Output: One-line backend fix + updated/new tests.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix create_skill default status + repair impacted tests</name>
  <files>backend/api/routes/admin_skills.py, backend/tests/api/test_admin_skills.py</files>
  <behavior>
    - Test (new): POST /api/admin/skills with valid payload returns 201 and body["status"] == "draft"
    - Test (updated): test_activate_skill must not assert the freshly-created skill is "active" — after creation it is "draft"; the test must transition draft→active via /activate without the intermediate disable step
    - Test (updated): test_bulk_status_update must assert the skill that was never bulk-disabled has status="draft" (not "active") after the fix
  </behavior>
  <action>
    1. In backend/api/routes/admin_skills.py, find the RegistryEntryCreate(...) call in create_skill() (around line 228-234). Change `status="active"` to `status="draft"`. Remove the comment "on_create downgrades to draft if tool_gaps present" — the default is now always draft.

    2. VERIFIED: The /activate endpoint (activate_skill_version, lines 796-836) has NO transition guard — it performs an unconditional `entry.status = "active"` for any current status as long as tool_gaps are absent. draft→active is fully permitted. No change to /activate is needed.

    3. In backend/tests/api/test_admin_skills.py, make the following changes:

       a. test_crud_flow (around line 147): Change
            `assert skill["is_active"] is True`
          to
            `assert skill["is_active"] is False`
          (A new skill has status='draft', which _entry_to_skill_response maps to is_active=False.)

       b. test_bulk_status_update (around line 440): Change
            `assert ... .json()["status"] == "active"`
          to
            `assert ... .json()["status"] == "draft"`
          for the third skill that was never bulk-disabled — after the fix it also starts as "draft".

       c. Add a new test function `test_create_skill_defaults_to_draft` immediately after the existing CRUD tests (before test_activate_skill). It should POST a minimal valid skill payload (name, version, skill_type, instruction_markdown) and assert response["status"] == "draft".

       d. Update `test_activate_skill`: The skill now starts as "draft" after POST. Remove the intermediate bulk-status disable step (lines 235-240) — it is no longer needed because the skill is already not "active". Call /activate directly on the newly-created draft skill and assert status == "active".

       e. Scan for any other test that asserts a newly-created skill has status "active" or is_active True immediately after POST, and fix those assertions to expect "draft" / False respectively.

    Do NOT change the /activate endpoint behaviour — it already promotes draft→active correctly. Do NOT change on_create() in SkillHandler — the fix is purely in create_skill().
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass (full suite). test_create_skill_defaults_to_draft asserts status="draft". test_activate_skill passes without the disable step. test_crud_flow asserts is_active is False. test_bulk_status_update asserts the untouched skill has status "draft".</done>
</task>

</tasks>

<verification>
cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q 2>&1 | tail -20

All tests pass. No regression in the broader test suite.
</verification>

<success_criteria>
- POST /api/admin/skills returns status="draft" for all new skills
- test_create_skill_defaults_to_draft exists and passes
- test_crud_flow passes with is_active asserted as False after POST
- test_bulk_status_update passes with untouched skill asserted as status="draft"
- test_activate_skill still passes (draft→active via /activate, no intermediate disable step needed)
- Full test suite passes with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/25-skill-builder-tool-resolver/25-06-SUMMARY.md`
</output>
