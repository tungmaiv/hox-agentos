---
phase: 25-skill-builder-tool-resolver
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/routes/admin_skills.py
  - backend/tests/api/test_admin_skills.py
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "A skill saved via POST /api/admin/skills returns status='draft'"
    - "test_activate_skill still passes after the fix"
    - "A new test asserts POST /api/admin/skills returns status='draft'"
  artifacts:
    - path: "backend/api/routes/admin_skills.py"
      provides: "create_skill endpoint with status='draft'"
      contains: "status=\"draft\""
    - path: "backend/tests/api/test_admin_skills.py"
      provides: "test asserting new skill starts as draft"
      contains: "test_create_skill_defaults_to_draft"
  key_links:
    - from: "admin_skills.py create_skill"
      to: "RegistryEntryCreate"
      via: "status='draft' kwarg"
      pattern: "status=\"draft\""
---

<objective>
Close UAT gap: skills saved via POST /api/admin/skills start life as `draft`, not `active`.

Purpose: The entire tool-resolver promotion pipeline (draft → pending_activation → active) is broken because the create endpoint bypasses it by defaulting to `active`. Tests 4-7 in the UAT were all skipped due to this cascade.

Output: One-line backend fix + updated/new tests.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix create_skill default status + repair impacted test</name>
  <files>backend/api/routes/admin_skills.py, backend/tests/api/test_admin_skills.py</files>
  <behavior>
    - Test (new): POST /api/admin/skills with valid payload returns 201 and body["status"] == "draft"
    - Test (updated): test_activate_skill must not assert the freshly-created skill is "active" — after creation it is "draft"; the test must transition draft→active via /activate without the intermediate disable step
  </behavior>
  <action>
    1. In backend/api/routes/admin_skills.py, find the RegistryEntryCreate(...) call in create_skill() (around line 228-234). Change `status="active"` to `status="draft"`. Remove the comment "on_create downgrades to draft if tool_gaps present" — the default is now always draft.

    2. In backend/tests/api/test_admin_skills.py:
       a. Add a new test function `test_create_skill_defaults_to_draft` immediately after the existing CRUD tests (before test_activate_skill). It should POST a minimal valid skill payload (name, version, skill_type, instruction_markdown) and assert response["status"] == "draft".
       b. Update `test_activate_skill`: The skill now starts as "draft" after POST. Remove the intermediate bulk-status disable step (lines 235-240) — it is no longer needed because the skill is already not "active". Call /activate directly on the newly-created draft skill and assert status == "active".
       c. Scan for any other test that asserts a newly-created skill has status "active" immediately after POST, and fix those assertions to expect "draft".

    Do NOT change the /activate endpoint behaviour — it already promotes draft→active correctly. Do NOT change on_create() in SkillHandler — the fix is purely in create_skill().
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_admin_skills.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in test_admin_skills.py pass. test_create_skill_defaults_to_draft asserts status="draft". test_activate_skill passes without the disable step.</done>
</task>

</tasks>

<verification>
cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/api/test_admin_skills.py -v -q 2>&1 | tail -20

All tests pass. No regression in the broader test suite:
cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q 2>&1 | tail -5
</verification>

<success_criteria>
- POST /api/admin/skills returns status="draft" for all new skills
- test_create_skill_defaults_to_draft exists and passes
- test_activate_skill still passes (draft→active via /activate)
- Full test suite passes with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/25-skill-builder-tool-resolver/25-06-SUMMARY.md`
</output>
