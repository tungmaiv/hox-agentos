---
phase: 21-skill-platform-c-dependency-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/skills/security_scanner.py
  - backend/tests/test_security_scanner.py
autonomous: true
requirements:
  - SKSEC-04

must_haves:
  truths:
    - "SecurityScanner.scan() computes a 6-factor weighted score summing to 100%"
    - "dependency_risk factor returns 0 when undeclared third-party imports are detected"
    - "data_flow_risk factor returns a reduced score when sensitive-read + outbound-write pattern is found"
    - "author_verification factor is removed — any reference to it in scan output raises KeyError"
    - "All existing SecurityScanner tests pass with the new factor set"
  artifacts:
    - path: "backend/skills/security_scanner.py"
      provides: "Enhanced scanner with _score_dependency_risk() and _score_data_flow_risk() methods"
      contains: "dependency_risk"
    - path: "backend/tests/test_security_scanner.py"
      provides: "Tests for 6-factor scoring, TestDependencyRisk, TestDataFlowRisk, updated TestWeightedScoring"
      contains: "TestDependencyRisk"
  key_links:
    - from: "backend/skills/security_scanner.py"
      to: "skill_data['scripts_content']"
      via: "_score_dependency_risk() reads scripts_content list from skill_data dict"
      pattern: "scripts_content"
    - from: "backend/skills/security_scanner.py"
      to: "sys.stdlib_module_names"
      via: "stdlib detection frozenset — Python 3.10+ required"
      pattern: "stdlib_module_names"
---

<objective>
Replace SecurityScanner's `author_verification` factor (always 50, no security value) with two new scoring factors: `dependency_risk` (20%) and `data_flow_risk` (10%). Rebalance all 6 factor weights to sum to 100%. Update all tests to reflect the new factor set.

Purpose: Provides the scoring infrastructure that SKSEC-01 (AST dependency scan in Plan 21-04) will exercise. The enhanced scanner is a drop-in replacement — all existing integration points in `admin_skills.py` and `skill_repos/service.py` call `.scan()` unchanged.
Output: Modified `security_scanner.py` with 6 scoring factors + updated `test_security_scanner.py` with TestDependencyRisk and TestDataFlowRisk classes.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-skill-platform-c-dependency-security-hardening/21-CONTEXT.md
@.planning/phases/21-skill-platform-c-dependency-security-hardening/21-RESEARCH.md

<interfaces>
<!-- Existing SecurityScanner interface — executor uses these directly -->

From backend/skills/security_scanner.py:
```python
@dataclass
class SecurityReport:
    score: int                   # 0-100 overall score
    factors: dict[str, int]      # factor_name -> individual score (0-100)
    recommendation: str          # "approve", "review", "reject"
    injection_matches: list[str]

class SecurityScanner:
    def scan(self, skill_data: dict[str, Any], source_url: str | None = None) -> SecurityReport: ...
    # Current factors (weights): source_reputation(30%), tool_scope(25%), prompt_safety(25%), complexity(10%), author_verification(10%)
    # After this plan: author_verification removed; 6 new factors at 25/20/20/5/20/10
```

Key fields in skill_data that the new factors consume:
```python
skill_data = {
    "scripts_content": [{"filename": str, "source": str}],  # NEW — added by Plan 21-04 importer
    "declared_dependencies": ["requests", "httpx"],           # NEW — added by Plan 21-04 importer
    "procedure_json": {"steps": [{"type": "tool", "tool": "email.fetch", ...}]},
    # ... existing fields unchanged
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _score_dependency_risk() and _score_data_flow_risk() — write tests first</name>
  <files>backend/skills/security_scanner.py, backend/tests/test_security_scanner.py</files>
  <behavior>
    - TestDependencyRisk::test_no_scripts_returns_100 — skill_data with no scripts_content key → factor = 100
    - TestDependencyRisk::test_undeclared_import_rejected — scripts_content has `import requests` but declared_dependencies=[] → factor = 0
    - TestDependencyRisk::test_declared_import_scores_positive — scripts_content has `import requests`, declared_dependencies=["requests"] → factor > 0
    - TestDependencyRisk::test_dangerous_package_penalty — declared_dependencies=["paramiko"], no undeclared imports → factor < 100
    - TestDependencyRisk::test_stdlib_import_not_penalized — scripts_content has `import os`, declared_dependencies=[] → os is stdlib → factor = 100 (not treated as undeclared)
    - TestDataFlowRisk::test_instructional_skill_returns_100 — no procedure_json → factor = 100
    - TestDataFlowRisk::test_exfiltration_pattern_detected — steps=[email.fetch, http.post] → score reduced by 60
    - TestDataFlowRisk::test_credential_pattern_in_template — prompt_template contains "api_key" → score reduced by 30
    - TestDataFlowRisk::test_clean_procedure_full_score — read-only tools, clean prompt → factor = 100
    - TestWeightedScoring::test_weights_sum_correctly — updated: uses 6 new factors at 0.25/0.20/0.20/0.05/0.20/0.10; removes author_verification reference
  </behavior>
  <action>
    Write failing tests first (RED), then implement.

    In `tests/test_security_scanner.py`:
    1. Add `TestDependencyRisk` class with 5 test methods covering the behaviors above.
    2. Add `TestDataFlowRisk` class with 4 test methods covering the behaviors above.
    3. Update `TestWeightedScoring::test_weights_sum_correctly` — remove `author_verification * 0.10` from the expected formula; replace with the 6-factor formula: `source_reputation*0.25 + tool_scope*0.20 + prompt_safety*0.20 + complexity*0.05 + dependency_risk*0.20 + data_flow_risk*0.10`.
    4. Confirm tests fail (RED). Then implement.

    In `security_scanner.py`:
    1. Add stdlib imports at top: `import ast`, `import sys`.
    2. Add module-level constants:
       - `_STDLIB_MODULES: frozenset[str] = sys.stdlib_module_names  # Python 3.10+`
       - `_DANGEROUS_PACKAGES: frozenset[str] = frozenset({"requests", "httpx", "paramiko", "cryptography", "pycryptodome", "scapy", "nmap"})`
       - `_OUTBOUND_TOOLS: frozenset[str] = frozenset({"http.post", "sandbox.run"})`
       - `_SENSITIVE_READ_TOOLS: frozenset[str] = frozenset({"email.fetch", "crm.get", "crm.list", "calendar.list"})`
       - `_CREDENTIAL_PATTERNS: list[re.Pattern[str]]` — compile 4 patterns: `r"api.?key"`, `r"password"`, `r"\btoken\b"`, `r"Bearer\s+\S+"` (all case-insensitive)
    3. Add helper `_extract_imports_from_source(source: str) -> set[str]` — uses `ast.parse()`, walks for `ast.Import` and `ast.ImportFrom` nodes, returns top-level module names (split on `.`). On `SyntaxError`, returns `{"<unparseable>"}`.
    4. Add `_score_dependency_risk(self, skill_data: dict[str, Any]) -> int` method:
       - If `scripts_content` is empty/absent → return 100
       - Extract all third-party imports from all scripts (exclude `_STDLIB_MODULES`)
       - Signal 3: undeclared = third_party_imports - declared_set; if undeclared → return 0
       - Signal 1: dangerous_found = declared_set & `_DANGEROUS_PACKAGES`; penalty = len(dangerous_found) * 20
       - Signal 2: bloat_score by count (0→100, 1-3→80, 4-10→50, 10+→20)
       - return max(0, bloat_score - danger_penalty)
    5. Add `_score_data_flow_risk(self, skill_data: dict[str, Any]) -> int` method:
       - If no `procedure_json` → return 100
       - Extract tool_names from tool steps
       - Signal 1: has_sensitive_read AND has_outbound → score -= 60
       - Signal 2: credential pattern found in any prompt_template → score -= 30 (break after first match)
       - Signal 3: if any tool in tool_names has `_classify_tool_scope(t) == 0` (admin) → score -= 40; elif <= 30 (sandbox) → score -= 20
       - return max(0, score) where score starts at 100
    6. Update `scan()` method:
       - Remove `_score_author_verification()` call
       - Add `factors["dependency_risk"] = self._score_dependency_risk(skill_data)`
       - Add `factors["data_flow_risk"] = self._score_data_flow_risk(skill_data)`
       - Update weighted formula: `source_reputation*0.25 + tool_scope*0.20 + prompt_safety*0.20 + complexity*0.05 + dependency_risk*0.20 + data_flow_risk*0.10`
    7. Update docstring at top of file to reflect 6 new factors and weights.
    8. Keep `_score_author_verification()` method removal clean — delete the method entirely.

    Note: `test_clean_skill_high_score` checks `report.score >= 80` — verify this still passes. Clean skill from agentskills.io with no scripts: source_rep=95*0.25=23.75, tool_scope=100*0.20=20, prompt_safety=100*0.20=20, complexity=100*0.05=5, dependency_risk=100*0.20=20, data_flow_risk=100*0.10=10 → total=98.75 → 99 ≥ 80. OK.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/test_security_scanner.py -x -q</automated>
  </verify>
  <done>All SecurityScanner tests pass including TestDependencyRisk (5 tests), TestDataFlowRisk (4 tests), updated TestWeightedScoring. No reference to author_verification in the 6-factor formula. Full suite baseline maintained: PYTHONPATH=. .venv/bin/pytest tests/ -q --ignore=tests/test_security_scanner.py shows no regressions.</done>
</task>

</tasks>

<verification>
- `PYTHONPATH=. .venv/bin/pytest tests/test_security_scanner.py -v` — all tests green, TestDependencyRisk and TestDataFlowRisk classes present
- `grep -n "author_verification" backend/skills/security_scanner.py` — returns nothing (method and references removed)
- `grep -n "dependency_risk\|data_flow_risk" backend/skills/security_scanner.py` — shows both factors in scan()
- `grep "0\.25\|0\.20\|0\.05\|0\.10" backend/skills/security_scanner.py` — shows new weight distribution
</verification>

<success_criteria>
- SecurityScanner.scan() computes a 6-factor score with weights summing to 100% (0.25+0.20+0.20+0.05+0.20+0.10)
- _score_dependency_risk() returns 0 when AST scan detects undeclared third-party import
- _score_data_flow_risk() returns reduced score for email.fetch → http.post pattern
- author_verification factor is gone from scanner code and from `factors` dict in SecurityReport
- All tests in test_security_scanner.py pass (existing + 9 new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/21-skill-platform-c-dependency-security-hardening/21-01-SUMMARY.md`
</output>
