---
phase: 27-admin-registry-edit-ui
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - frontend/src/app/(authenticated)/admin/mcp-servers/[id]/page.tsx
  - frontend/src/app/(authenticated)/admin/skills/[id]/page.tsx
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - REG-04

must_haves:
  truths:
    - "Admin can view and edit MCP server details through structured form fields"
    - "Admin can test MCP connection from the detail page with current form values"
    - "Connection test result displays inline as success (latency + tool count) or failure (error + hint)"
    - "Admin can view and edit skill details through structured form fields"
    - "Skill instruction markdown has a Preview toggle rendering markdown"
    - "MCP Tools tab shows read-only list of tools from the server"
  artifacts:
    - path: "frontend/src/app/(authenticated)/admin/mcp-servers/[id]/page.tsx"
      provides: "MCP server detail page with Overview, Connection, Tools tabs"
      min_lines: 200
    - path: "frontend/src/app/(authenticated)/admin/skills/[id]/page.tsx"
      provides: "Enhanced skill detail page with form editing"
      min_lines: 200
  key_links:
    - from: "frontend/src/app/(authenticated)/admin/mcp-servers/[id]/page.tsx"
      to: "/api/admin/mcp-servers/test"
      via: "POST with current form URL/auth values"
      pattern: "fetch.*mcp-servers/test"
    - from: "frontend/src/app/(authenticated)/admin/mcp-servers/[id]/page.tsx"
      to: "frontend/src/components/admin/registry-detail-layout.tsx"
      via: "import RegistryDetailLayout"
      pattern: "RegistryDetailLayout"
    - from: "frontend/src/app/(authenticated)/admin/skills/[id]/page.tsx"
      to: "/api/registry/{id}"
      via: "PUT on save"
      pattern: "fetch.*api/registry"
---

<objective>
Create MCP server detail page with connection testing, and enhance the existing skill detail page with form-based editing. Both use shared components from Plan 01.

Purpose: MCP servers get a detail page with live connection testing; skills upgrade from read-only display to editable forms with markdown preview.
Output: MCP server detail page at /admin/mcp-servers/[id], enhanced skill detail page at /admin/skills/[id].
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-admin-registry-edit-ui/27-CONTEXT.md
@.planning/phases/27-admin-registry-edit-ui/27-01-SUMMARY.md

@frontend/src/app/(authenticated)/admin/skills/[id]/page.tsx
@frontend/src/lib/admin-types.ts
@backend/core/schemas/registry.py
@backend/api/routes/mcp_servers.py

<interfaces>
<!-- Components from Plan 01 -->

From frontend/src/components/admin/registry-detail-layout.tsx:
```typescript
interface RegistryDetailLayoutProps {
  entry: RegistryEntry;
  backHref: string;
  backLabel: string;
  tabs: { id: string; label: string }[];
  activeTab: string;
  onTabChange: (tab: string) => void;
  children: React.ReactNode;
  hasChanges?: boolean;
  saving?: boolean;
  onSave?: () => void;
  onDiscard?: () => void;
}
export function RegistryDetailLayout(props: RegistryDetailLayoutProps): JSX.Element;
```

From frontend/src/lib/registry-schemas.ts:
```typescript
export const mcpServerFormSchema: z.ZodObject<...>;
export const skillFormSchema: z.ZodObject<...>;
export type McpServerFormData = z.infer<typeof mcpServerFormSchema>;
export type SkillFormData = z.infer<typeof skillFormSchema>;
export function validateField(schema: z.ZodObject<any>, fieldName: string, value: unknown): string | null;
```

Backend POST /api/admin/mcp-servers/test:
```python
class McpTestRequest(BaseModel):
    url: str
    auth_token: str | None = None

# Response: { success: bool, latency_ms: int, tool_count: int | None, error: str | None, hint: str | None }
```

Backend GET /api/admin/mcp-servers/{id}/health:
```python
# Response: { server_id: str, name: str, reachable: bool, latency_ms: int }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP server detail page with connection testing</name>
  <files>frontend/src/app/(authenticated)/admin/mcp-servers/[id]/page.tsx</files>
  <action>
    Create `frontend/src/app/(authenticated)/admin/mcp-servers/[id]/page.tsx` as a "use client" component:

    **Data loading:**
    - Fetch `GET /api/registry/${id}` on mount, map with `mapSnakeToCamel`
    - MCP server config fields are in `entry.config`: url, auth_token_hex (encrypted — never display), is_active

    **Tabs:** Overview, Connection, Tools (per CONTEXT.md decision)

    **Overview tab:**
    - Read-only fields: ID (mono), Name (read-only), Status, Owner ID, Created, Updated
    - Editable: Display Name (text input), Description (textarea)

    **Connection tab — MCP-specific:**
    - URL: text input — from `config.url` (required, validated as URL by Zod)
    - Auth Token: password input — always empty (never pre-filled from encrypted storage). Placeholder: "Enter new token to update". Only sent in save if non-empty.
    - Status: dropdown (active/disabled/deprecated)
    - **Test Connection button:**
      - On click: POST to `/api/admin/mcp-servers/test` with `{ url: currentFormUrl, auth_token: currentFormAuthToken || null }`
      - Uses CURRENT (possibly unsaved) form values — per CONTEXT.md decision
      - While testing: button shows spinner, text changes to "Testing..."
      - Result displayed as inline card below button:
        - Success: green border card showing "Connected" + latency (e.g., "142ms") + tool count (e.g., "5 tools available")
        - Failure: red border card showing error message + hint text in gray
      - Result card stays visible until next test or page navigation

    **Tools tab:**
    - Shows a read-only list of tools from this MCP server
    - Fetch tool list: GET /api/registry?type=tool, filter client-side where `config.mcp_server_id === entry.id`
    - Display as a simple table: Name, Description, Input Schema (collapsible JSON)
    - "Refresh" button at top right to re-fetch
    - If no tools: "No tools registered from this server" message

    **Change tracking + save:**
    - Same pattern as agent/tool detail pages
    - Save builds config: `{ ...existingConfig, url: formData.url }` — only include auth_token if non-empty (and it will be re-encrypted by backend)
    - For auth_token updates: the save payload sends `config.auth_token` as plaintext; backend encrypts it
    - After save, clear the auth token input field

    **Use `RegistryDetailLayout` for consistent shell.**
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    - /admin/mcp-servers/[id] renders with Overview, Connection, Tools tabs
    - Connection tab has URL input, auth token input, Test Connection button
    - Test Connection posts current form values and shows inline result card
    - Tools tab lists tools registered from this MCP server
    - Save updates config via PUT /api/registry/{id}
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance skill detail page with form editing</name>
  <files>frontend/src/app/(authenticated)/admin/skills/[id]/page.tsx</files>
  <action>
    Rewrite the existing skill detail page at `frontend/src/app/(authenticated)/admin/skills/[id]/page.tsx` to use the shared layout and support form editing. The current page is read-only with raw JSON — transform it to match the form-based pattern.

    **Use `RegistryDetailLayout` for shell** (replacing the hand-rolled header/tabs).

    **Tabs:** Overview, Config, Scan Results (keep existing 3-tab structure per CONTEXT.md)

    **Overview tab:**
    - Read-only fields: ID (mono), Name (read-only), Skill Type badge, Source Type, Slash Command, Category, Tags, Security Score, Owner, Created, Updated
    - Editable: Display Name (text input), Description (textarea), Status dropdown

    **Config tab — skill-specific fields:**
    - Skill Type: dropdown (instructional/procedural) — changing this affects which content field is required
    - Instruction Markdown: textarea (tall, min 8 rows) with a "Preview" toggle button
      - When Preview is ON: render markdown as HTML below the textarea using `react-markdown` (check if already in deps; if not, render as simple `dangerouslySetInnerHTML` with basic sanitization or just show the raw text in a styled pre block — do NOT add new dependencies without checking)
      - Visible when skill_type=instructional
    - Procedure JSON: JSON textarea (collapsible) — visible when skill_type=procedural
    - Slash Command: text input
    - Category: text input
    - Tags: comma-separated text input (stored as string array)
    - Allowed Tools: comma-separated text input (stored as string array)
    - "Advanced (raw JSON)" collapsible section for remaining config keys

    **Scan Results tab:**
    - Keep the EXISTING scan results display (security score, recommendation badge, bandit issues, pip-audit issues, findings) — this is already well-implemented
    - Just wrap it in the new layout structure

    **Change tracking + save:**
    - Initialize formData from entry.config fields on load
    - Use `skillFormSchema` for validation on blur
    - Save builds config merging form fields back: `{ ...existingConfig, instruction_markdown: ..., skill_type: ..., slash_command: ..., category: ..., tags: ..., allowed_tools: ... }`
    - PUT to `/api/registry/${id}`

    **Markdown preview:**
    - Check if `react-markdown` is in `frontend/package.json`. If yes, import and use it.
    - If not available, use a simple approach: render in a `<div className="prose prose-sm">` with `dangerouslySetInnerHTML={{ __html: text }}` (basic — markdown will show as-is but in a styled container). Do NOT add react-markdown as a new dependency unless it already exists.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    - /admin/skills/[id] uses RegistryDetailLayout with consistent look
    - Config tab has structured form fields for instruction_markdown, skill_type, category, tags
    - Instruction markdown has Preview toggle
    - Scan Results tab preserved with existing security display
    - Save updates skill config via PUT /api/registry/{id}
    - Zod validation on blur with inline errors
  </done>
</task>

</tasks>

<verification>
- `cd /home/tungmv/Projects/hox-agentos/frontend && pnpm exec tsc --noEmit` passes
- `cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build` passes
</verification>

<success_criteria>
- MCP server detail page at /admin/mcp-servers/[id] with Overview, Connection (test button), Tools tabs
- Test Connection button uses current form values, shows inline result card with success/failure
- Skill detail page upgraded from read-only to form-based editing with RegistryDetailLayout
- Instruction markdown has Preview toggle
- All 4 registry types now have detail pages with consistent layout (REG-01 complete)
- All 4 types support form-based editing (REG-02 complete)
- Type-specific config fields are real form inputs (REG-03 complete)
- MCP connection test works from detail page (REG-04 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/27-admin-registry-edit-ui/27-03-SUMMARY.md`
</output>
