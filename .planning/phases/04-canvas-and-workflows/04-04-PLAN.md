---
phase: 04-canvas-and-workflows
plan: "04"
type: tdd
wave: 4
depends_on:
  - "04-03"
files_modified:
  - backend/scheduler/tasks/workflow_execution.py
  - backend/tests/scheduler/test_hitl_resume.py
  - frontend/src/components/canvas/workflow-canvas.tsx
  - frontend/src/components/canvas/node-palette.tsx
  - frontend/src/components/canvas/run-controls.tsx
  - frontend/src/components/canvas/nodes/trigger-node.tsx
  - frontend/src/components/canvas/nodes/agent-node.tsx
  - frontend/src/components/canvas/nodes/tool-node.tsx
  - frontend/src/components/canvas/nodes/condition-node.tsx
  - frontend/src/components/canvas/nodes/hitl-approval-node.tsx
  - frontend/src/components/canvas/nodes/channel-output-node.tsx
  - frontend/src/hooks/use-workflow-run.ts
  - frontend/src/hooks/use-pending-hitl.ts
autonomous: false
requirements:
  - WKFL-01
  - WKFL-05

must_haves:
  truths:
    - "AsyncPostgresSaver replaces MemorySaver in execute_workflow — HITL pause state persists across worker restarts"
    - "Resume path: POST /approve or /reject injects hitl_result into saved checkpoint via aupdate_state(), then re-invokes the graph"
    - "Each canvas node renders a colored status ring: idle (gray) | running (blue pulse) | completed (green) | failed (red) | awaiting_approval (amber)"
    - "hitl_approval_node renders Approve/Reject buttons inline when status is awaiting_approval"
    - "use-workflow-run subscribes to SSE stream and maintains Map<node_id, NodeStatus> in React state"
    - "NodePalette allows dragging nodes onto the canvas (onDrop creates a new node at drop position)"
    - "RunControls shows Run button; clicking POST /api/workflows/{id}/run and subscribes to SSE"
  artifacts:
    - path: "backend/scheduler/tasks/workflow_execution.py"
      provides: "AsyncPostgresSaver replaces MemorySaver; resume path via aupdate_state()"
      contains: "AsyncPostgresSaver"
    - path: "frontend/src/hooks/use-workflow-run.ts"
      provides: "SSE subscription → Map<node_id, NodeStatus>"
      contains: "useWorkflowRun"
    - path: "frontend/src/components/canvas/nodes/hitl-approval-node.tsx"
      provides: "Approve/Reject buttons when awaiting_approval"
      contains: "awaiting_approval"
  key_links:
    - from: "frontend/src/hooks/use-workflow-run.ts"
      to: "frontend/src/components/canvas/workflow-canvas.tsx"
      via: "nodeStatuses Map passed as prop; each node reads its own status"
      pattern: "nodeStatuses"
    - from: "backend execute_workflow resume path"
      to: "aupdate_state() + re-invoke with None input"
      via: "compiled.aupdate_state(config, {'hitl_result': decision}); compiled.ainvoke(None, config)"
      pattern: "aupdate_state"
---

<objective>
Replace MemorySaver with AsyncPostgresSaver for real HITL persistence, wire the resume API, then build the complete canvas UI — all 6 node renderers with status rings, NodePalette drag-and-drop, RunControls, and the SSE-driven hooks.

New Python deps required: uv add langgraph-checkpoint-postgres "psycopg[binary]"
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-02-27-phase-4-canvas-workflows-design.md
@docs/plans/2026-02-27-phase-4-04-04-hitl-canvas.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Install deps + AsyncPostgresSaver HITL persistence</name>
  <files>
    backend/scheduler/tasks/workflow_execution.py
    backend/tests/scheduler/test_hitl_resume.py
  </files>
  <action>
    Full TDD spec in referenced plan doc (Task 1).

    1. Install deps: cd backend && uv add langgraph-checkpoint-postgres "psycopg[binary]"

    2. Write failing test test_execute_workflow_with_hitl_result_calls_update_state (RED):
       - Mock WorkflowRun with status="paused_hitl", checkpoint_id set
       - Mock compiled graph's aupdate_state and astream_events
       - Call execute_workflow with hitl_result="approved"
       - Assert aupdate_state called with {"hitl_result": "approved"}
       - Assert astream_events called with None input (resume from checkpoint)

    3. Modify compile_workflow_to_stategraph() in graphs.py to accept an optional
       checkpointer parameter. execute_workflow passes AsyncPostgresSaver(conn=...) as checkpointer.
       For tests that don't pass a checkpointer, fall back to MemorySaver.

    4. In execute_workflow:
       - Build AsyncPostgresSaver from settings.database_url (sync psycopg connection)
       - Pass to compile_workflow_to_stategraph as checkpointer
       - Set thread_id = str(run_id) as LangGraph config
       - Store checkpoint_id = str(run_id) in WorkflowRun

    5. Resume path (when hitl_result is not None):
       - Re-compile graph with same checkpointer and thread_id
       - await compiled.aupdate_state(config, {"hitl_result": decision})
       - await compiled.ainvoke(None, config)  # resumes from checkpoint

    KNOWN ISSUE: CopilotKit + LangGraph HITL has a ZodError — we use graph interrupt() nodes
    (NOT the CopilotKit HITL built-in). This approach is correct per STATE.md decisions.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/scheduler/test_hitl_resume.py -v 2>&1 | tail -12</automated>
    <manual>HITL resume tests PASSED. AsyncPostgresSaver imported without error.</manual>
    <sampling_rate>after each change to workflow_execution.py</sampling_rate>
  </verify>
  <done>
    - langgraph-checkpoint-postgres and psycopg[binary] in pyproject.toml
    - AsyncPostgresSaver used in execute_workflow
    - Resume path calls aupdate_state() then ainvoke(None)
    - test_hitl_resume.py: all PASSED
  </done>
</task>

<task type="tdd">
  <name>Task 2: Frontend deps + TypeScript types</name>
  <files>
    frontend/src/lib/workflow-types.ts
  </files>
  <action>
    1. Verify @xyflow/react is installed (from 04-01). If not: cd frontend && pnpm add @xyflow/react
    2. Create frontend/src/lib/workflow-types.ts with:
       - NodeStatus: "idle" | "running" | "completed" | "failed" | "awaiting_approval"
       - WorkflowNodeData: { label: string; nodeType: string; config: Record<string, unknown>; status?: NodeStatus }
       - WorkflowEdgeData: Record<string, unknown>
       - RunEvent: { event: string; node_id?: string; output?: unknown; message?: string; error?: string }
    3. No tests needed for types — TypeScript compiler validates at build time.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build 2>&1 | grep -E "error|Error" | head -5</automated>
    <manual>No TypeScript errors from workflow-types.ts</manual>
    <sampling_rate>after creating types file</sampling_rate>
  </verify>
  <done>
    - frontend/src/lib/workflow-types.ts exists with all 4 types
    - pnpm run build: no errors from this file
  </done>
</task>

<task type="tdd">
  <name>Task 3: use-workflow-run hook — SSE subscription + node status map</name>
  <files>
    frontend/src/hooks/use-workflow-run.ts
    frontend/src/hooks/use-pending-hitl.ts
  </files>
  <action>
    Full spec in referenced plan doc (Tasks 2–3).

    use-workflow-run.ts:
    - "use client"
    - useWorkflowRun(runId: string | null) hook
    - Opens EventSource to /api/workflows/runs/{runId}/events when runId is set
    - Parses each event data as RunEvent
    - Maintains nodeStatuses: Map<string, NodeStatus> in useState
    - Updates map on node_started (→ "running"), node_completed (→ "completed"),
      node_failed (→ "failed"), hitl_paused (→ "awaiting_approval" for that node)
    - Sets isComplete=true on workflow_completed or workflow_failed
    - Closes EventSource on cleanup / isComplete
    - Returns { nodeStatuses, isComplete, pendingHitlNodeId }

    use-pending-hitl.ts:
    - Polls GET /api/workflows/runs/pending-hitl every 30s
    - Returns count: number for nav badge
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build 2>&1 | grep -E "error TS" | head -10</automated>
    <manual>No TypeScript errors. Hooks compile cleanly.</manual>
    <sampling_rate>after each hook</sampling_rate>
  </verify>
  <done>
    - use-workflow-run.ts: EventSource + nodeStatuses Map
    - use-pending-hitl.ts: polling hook
    - Both compile with strict TypeScript
  </done>
</task>

<task type="tdd">
  <name>Task 4: All 6 node renderers with status rings</name>
  <files>
    frontend/src/components/canvas/nodes/trigger-node.tsx
    frontend/src/components/canvas/nodes/agent-node.tsx
    frontend/src/components/canvas/nodes/tool-node.tsx
    frontend/src/components/canvas/nodes/condition-node.tsx
    frontend/src/components/canvas/nodes/hitl-approval-node.tsx
    frontend/src/components/canvas/nodes/channel-output-node.tsx
  </files>
  <action>
    Full spec in referenced plan doc (Tasks 4–5).

    Each node renderer:
    - "use client"
    - Accepts NodeProps<WorkflowNodeData> from @xyflow/react
    - Renders a card with label + config summary
    - Status ring via Tailwind ring utility: idle=gray, running=blue animate-pulse, completed=green, failed=red, awaiting_approval=amber
    - Reads status from data.status

    hitl-approval-node.tsx additionally:
    - When data.status === "awaiting_approval": renders Approve (green) + Reject (red) buttons
    - Buttons call POST /api/workflows/runs/{runId}/approve or /reject
    - runId passed via data.config.runId (set by WorkflowCanvas when run starts)

    condition-node.tsx:
    - Two output handles: true (right-bottom, green) and false (right-top, red)
    - Use <Handle type="source" position={Position.Right} id="true" /> etc.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build 2>&1 | grep -E "error TS" | head -10</automated>
    <manual>All 6 node files compile. No TypeScript errors.</manual>
    <sampling_rate>after each node renderer</sampling_rate>
  </verify>
  <done>
    - All 6 node renderer files exist
    - Each has a status ring driven by data.status
    - hitl-approval-node renders Approve/Reject when awaiting_approval
    - condition-node has two output handles
    - pnpm run build: 0 TypeScript errors
  </done>
</task>

<task type="tdd">
  <name>Task 5: WorkflowCanvas, NodePalette, RunControls — wire everything</name>
  <files>
    frontend/src/components/canvas/workflow-canvas.tsx
    frontend/src/components/canvas/node-palette.tsx
    frontend/src/components/canvas/run-controls.tsx
  </files>
  <action>
    Full spec in referenced plan doc (Tasks 6–7).

    workflow-canvas.tsx (replace stub from 04-01):
    - "use client"
    - Accepts workflowId: string prop
    - Uses useWorkflow(workflowId) for CRUD ops
    - Uses useWorkflowRun(activeRunId) for SSE status
    - Registers all 6 node types in nodeTypes map: { trigger_node: TriggerNode, ... }
    - Passes nodeStatuses to node data so each node knows its status
    - onDrop: creates new node at drop position
    - onConnect: adds edge
    - Auto-saves definition_json on change (debounced 1s PUT /api/workflows/{id})

    node-palette.tsx:
    - Left sidebar with 6 draggable node type entries
    - onDragStart sets dataTransfer: { nodeType: string }

    run-controls.tsx:
    - "Run" button: POST /api/workflows/{id}/run → sets activeRunId → triggers SSE subscription
    - Shows run status: idle | running | paused_hitl | completed | failed
    - "Pending HITL" badge using usePendingHitl count
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build 2>&1 | tail -15</automated>
    <manual>Build succeeds. /workflows/[id] renders full canvas with palette, run controls, node status rings. Dragging a node from palette onto canvas creates it. Run button fires and SSE updates node colors.</manual>
    <sampling_rate>after each component</sampling_rate>
  </verify>
  <done>
    - workflow-canvas.tsx: registers all 6 node types, wires SSE statuses
    - node-palette.tsx: 6 draggable entries
    - run-controls.tsx: run button + status display
    - pnpm run build: 0 TypeScript errors
    - Canvas loads at /workflows/[id] with full interactivity
  </done>
</task>

<task type="auto">
  <name>Task 6: Full backend suite green</name>
  <files></files>
  <action>
    cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q 2>&1 | tail -5</automated>
    <manual>N passed, 0 failed</manual>
    <sampling_rate>after all tasks complete</sampling_rate>
  </verify>
  <done>
    Full suite: N passed, 0 failed, 0 errors.
  </done>
</task>

</tasks>

<verification>
Plan 04-04 is complete when:

1. **WKFL-05**: HITL approval node pauses the graph (`interrupt()`), persists state via `AsyncPostgresSaver`, and resume API injects the decision and continues
2. **WKFL-01**: Canvas renders all 6 node types with status rings; drag-and-drop from palette creates nodes
3. **End-to-end manual check**: Create a 2-node workflow (trigger → hitl), Run it, see node turn amber, click Approve, see node turn green

Automated gates:
```bash
cd /home/tungmv/Projects/hox-agentos/backend
PYTHONPATH=. .venv/bin/pytest tests/scheduler/test_hitl_resume.py -v
PYTHONPATH=. .venv/bin/pytest tests/ -q
cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build
```
</verification>

<success_criteria>
- AsyncPostgresSaver used in execute_workflow (not MemorySaver)
- Resume path: aupdate_state() then ainvoke(None, config) with same thread_id
- All 6 node renderers exist with status rings
- hitl-approval-node renders Approve/Reject buttons when status == "awaiting_approval"
- use-workflow-run: Map<node_id, NodeStatus> updated from SSE events
- pnpm run build: 0 TypeScript errors
- pytest tests/ -q: 0 failures
</success_criteria>

<output>
After completion, create .planning/phases/04-canvas-and-workflows/04-04-SUMMARY.md following the summary template.
</output>
