---
phase: 04-canvas-and-workflows
plan: "02"
type: tdd
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/agents/workflow_state.py
  - backend/agents/node_handlers.py
  - backend/agents/graphs.py
  - backend/tests/agents/test_workflow_state.py
  - backend/tests/agents/test_node_handlers.py
  - backend/tests/agents/test_compiler.py
autonomous: true
requirements:
  - WKFL-02
  - WKFL-08

must_haves:
  truths:
    - "WorkflowState TypedDict has: run_id, user_context, node_outputs (dict), current_output, hitl_result"
    - "compile_workflow_to_stategraph(definition_json, user_context) in graphs.py topologically sorts nodes and builds a StateGraph[WorkflowState]"
    - "condition_node compiles to add_conditional_edges with true/false branches"
    - "hitl_approval_node handler calls langgraph interrupt() — graph pauses at that node"
    - "Condition expression evaluator is sandboxed — no eval(), uses re-based parsing of output.<field> comparisons only"
    - "agent_node and tool_node handlers are functional stubs in this plan (real wiring in 04-03)"
    - "Full test suite passes: N passed, 0 failed"
  artifacts:
    - path: "backend/agents/workflow_state.py"
      provides: "WorkflowState TypedDict"
      contains: "class WorkflowState"
    - path: "backend/agents/node_handlers.py"
      provides: "Handler registry: get_handler(node_type) -> async handler fn"
      contains: "NODE_HANDLER_REGISTRY"
    - path: "backend/agents/graphs.py"
      provides: "compile_workflow_to_stategraph() added alongside existing compile_workflow_to_stategraph stub or new function"
      contains: "compile_workflow_to_stategraph"
  key_links:
    - from: "backend/agents/graphs.py"
      to: "backend/agents/node_handlers.py"
      via: "get_handler(node['type']) called during compilation"
      pattern: "from agents.node_handlers import get_handler"
    - from: "hitl_approval_node handler"
      to: "langgraph interrupt()"
      via: "from langgraph.types import interrupt; interrupt(message) inside handler"
      pattern: "interrupt\\("
---

<objective>
Build the core compiler that turns a React Flow `definition_json` into an executable LangGraph `StateGraph`. Includes WorkflowState, sandboxed condition evaluator, node handler registry, and the compiler itself.

agent_node and tool_node handlers are functional stubs here — full wiring happens in 04-03.
The compiler uses MemorySaver as the checkpointer for now — AsyncPostgresSaver replaces it in 04-04.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-02-27-phase-4-canvas-workflows-design.md
@docs/plans/2026-02-27-phase-4-04-02-compiler.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED→GREEN): WorkflowState TypedDict</name>
  <files>
    backend/agents/workflow_state.py
    backend/tests/agents/test_workflow_state.py
  </files>
  <action>
    Full TDD spec in referenced plan doc (Task 1).

    1. Write failing tests (RED): test_workflow_state_has_required_keys, test_workflow_state_node_outputs_accumulates
    2. Create backend/agents/workflow_state.py:
       - WorkflowState(TypedDict) with fields: run_id: UUID, user_context: dict, node_outputs: dict[str, Any], current_output: Any, hitl_result: str | None
    3. Run tests → GREEN

    No LangGraph reducers on node_outputs — plain dict, manually accumulated by handlers.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/agents/test_workflow_state.py -v 2>&1 | tail -8</automated>
    <manual>All tests PASSED</manual>
    <sampling_rate>after creating workflow_state.py</sampling_rate>
  </verify>
  <done>
    - backend/agents/workflow_state.py exists
    - test_workflow_state.py: all PASSED
  </done>
</task>

<task type="tdd">
  <name>Task 2 (RED→GREEN): Sandboxed condition evaluator</name>
  <files>
    backend/agents/node_handlers.py
    backend/tests/agents/test_node_handlers.py
  </files>
  <action>
    Full TDD spec in referenced plan doc (Task 2).

    Implement _evaluate_condition(expression: str, state: WorkflowState) -> bool in node_handlers.py.

    Supported syntax ONLY (no eval, no exec):
    - output.<field> > <number>
    - output.<field> < <number>
    - output.<field> == <value>
    - output.<field> != <value>
    - output.<field>.contains("<string>")
    - output.<field>.is_empty()
    - not output.<field>

    operand: output.<field> reads state["current_output"][field].
    Unsupported expression → raise ValueError.
    Use re module for parsing. No eval() anywhere.

    Tests: see referenced plan doc for full test list including edge cases.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/agents/test_node_handlers.py -k condition -v 2>&1 | tail -10</automated>
    <manual>All condition evaluator tests PASSED</manual>
    <sampling_rate>after implementing _evaluate_condition</sampling_rate>
  </verify>
  <done>
    - _evaluate_condition exists in node_handlers.py
    - No eval() or exec() anywhere in the file
    - Condition evaluator tests: all PASSED
  </done>
</task>

<task type="tdd">
  <name>Task 3 (RED→GREEN): Node handler registry — all 6 node types</name>
  <files>
    backend/agents/node_handlers.py
    backend/tests/agents/test_node_handlers.py
  </files>
  <action>
    Full TDD spec in referenced plan doc (Tasks 3–4).

    Create NODE_HANDLER_REGISTRY: dict[str, Callable] and get_handler(node_type: str) -> Callable.
    Implement handlers for all 6 node types:

    - trigger_node: no-op (entry point, just passes state through)
    - agent_node: STUB — returns {"success": True, "output": {"stub": True}} + logs warning
    - tool_node: STUB — looks up tool in tool_registry, returns {"success": False, "error": "not registered"} for unknown tools
    - condition_node: calls _evaluate_condition → returns {"result": True|False}
    - hitl_approval_node: calls interrupt(node_config["message"]) from langgraph.types
    - channel_output_node: STUB — returns {"success": True, "output": {"delivered": False, "stub": True}}

    Handler signature: async def handler(node_config: dict, state: WorkflowState) -> dict

    Tests: handler exists for each type, unknown type raises KeyError, condition handler returns bool.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/agents/test_node_handlers.py -v 2>&1 | tail -15</automated>
    <manual>All handler tests PASSED</manual>
    <sampling_rate>after each handler implementation</sampling_rate>
  </verify>
  <done>
    - NODE_HANDLER_REGISTRY has all 6 node types
    - get_handler() raises KeyError for unknown types
    - All handler tests PASSED
  </done>
</task>

<task type="tdd">
  <name>Task 4 (RED→GREEN): compile_workflow_to_stategraph()</name>
  <files>
    backend/agents/graphs.py
    backend/tests/agents/test_compiler.py
  </files>
  <action>
    Full TDD spec in referenced plan doc (Tasks 5–6).

    Add compile_workflow_to_stategraph(definition_json: dict, user_context: dict) to backend/agents/graphs.py.
    (graphs.py already exists from Phase 2 — add the new function without breaking existing compile_workflow_to_stategraph if it exists, or replace the existing stub.)

    Algorithm:
    1. Assert definition_json["schema_version"] == "1.0" — raise ValueError if not
    2. Topological sort nodes from definition_json["nodes"] and definition_json["edges"]
    3. Build StateGraph[WorkflowState]
    4. For each node in topo order:
       - get_handler(node["type"]) → wrap as async LangGraph node
       - condition_node → add_conditional_edges(node_id, lambda state: "true" if result else "false")
       - hitl_approval_node → add_node() with interrupt() inside handler (stays as regular node)
       - others → add_node() + add_edge()
    5. Set entry_point from trigger_node
    6. Compile with MemorySaver() as checkpointer for now (replaced with AsyncPostgresSaver in 04-04)
    7. Return compiled graph

    Tests: empty graph compiles, wrong schema_version raises ValueError, linear 3-node graph compiles,
    condition node produces two branches.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/agents/test_compiler.py -v 2>&1 | tail -15</automated>
    <manual>All compiler tests PASSED</manual>
    <sampling_rate>after implementing compile_workflow_to_stategraph</sampling_rate>
  </verify>
  <done>
    - compile_workflow_to_stategraph() exists in backend/agents/graphs.py
    - ValueError raised for wrong schema_version
    - Compiler tests: all PASSED
  </done>
</task>

<task type="auto">
  <name>Task 5: Full suite green</name>
  <files></files>
  <action>
    cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q

    If failures: investigate root cause. Do not skip or xfail.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q 2>&1 | tail -5</automated>
    <manual>N passed, 0 failed</manual>
    <sampling_rate>after all tasks complete</sampling_rate>
  </verify>
  <done>
    Full suite: N passed, 0 failed, 0 errors.
  </done>
</task>

</tasks>

<verification>
Plan 04-02 is complete when:

1. **WKFL-02 partial**: `compile_workflow_to_stategraph()` builds a runnable `StateGraph` from a `definition_json`
2. **Condition safety**: `_evaluate_condition()` uses regex parsing, zero `eval()` calls
3. **HITL stub**: `hitl_approval_node` handler calls `interrupt()` — graph pauses correctly
4. **Full suite**: `pytest tests/ -q` shows 0 failures

Automated gates:
```bash
cd /home/tungmv/Projects/hox-agentos/backend
PYTHONPATH=. .venv/bin/pytest tests/agents/test_workflow_state.py tests/agents/test_node_handlers.py tests/agents/test_compiler.py -v
PYTHONPATH=. .venv/bin/pytest tests/ -q
```
</verification>

<success_criteria>
- backend/agents/workflow_state.py: WorkflowState TypedDict with 5 required fields
- backend/agents/node_handlers.py: NODE_HANDLER_REGISTRY with all 6 handlers, no eval()
- backend/agents/graphs.py: compile_workflow_to_stategraph() present and tested
- ValueError raised when schema_version != "1.0"
- hitl_approval_node calls interrupt() from langgraph.types
- pytest tests/ -q: 0 failures
</success_criteria>

<output>
After completion, create .planning/phases/04-canvas-and-workflows/04-02-SUMMARY.md following the summary template.
</output>
