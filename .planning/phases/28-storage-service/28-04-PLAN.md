---
phase: 28-storage-service
plan: 04
type: execute
wave: 4
depends_on:
  - 28-03
files_modified:
  - backend/api/routes/admin_storage.py
  - backend/api/routes/storage.py
  - backend/main.py
  - frontend/src/app/(authenticated)/admin/storage/page.tsx
  - frontend/src/components/user-notification-bell.tsx
  - frontend/src/app/(authenticated)/layout.tsx
autonomous: false
requirements:
  - STOR-04
  - STOR-06

user_setup:
  - service: minio
    why: "MinIO credentials needed for local dev and production"
    env_vars:
      - name: MINIO_ROOT_USER
        source: "Choose any username (e.g., 'minio_admin') — set in root .env"
      - name: MINIO_ROOT_PASSWORD
        source: "Choose any strong password — set in root .env"
      - name: MINIO_ACCESS_KEY
        source: "Copy MINIO_ROOT_USER value into backend/.env as MINIO_ACCESS_KEY"
      - name: MINIO_SECRET_KEY
        source: "Copy MINIO_ROOT_PASSWORD value into backend/.env as MINIO_SECRET_KEY"

must_haves:
  truths:
    - "Admin can view and edit storage settings (max file size, allowed MIME types) from admin panel"
    - "User notification bell in nav rail shows badge when new shares arrive"
    - "Notification bell dismisses/marks-as-read on click, badge count decrements"
    - "MinIO service starts and bucket blitz-files is created automatically (via minio-init)"
    - "Full file manager flow works end-to-end: upload, share, add to memory"
  artifacts:
    - path: "backend/api/routes/admin_storage.py"
      provides: "Admin endpoints for storage settings (max size, allowed MIME types)"
      exports: ["router"]
    - path: "frontend/src/components/user-notification-bell.tsx"
      provides: "Per-user notification bell with badge count, dropdown of recent notifications"
    - path: "frontend/src/app/(authenticated)/admin/storage/page.tsx"
      provides: "Admin settings page for file storage configuration"
  key_links:
    - from: "frontend/src/components/user-notification-bell.tsx"
      to: "/api/storage/notifications"
      via: "fetch on mount + polling or bell click"
      pattern: "fetch.*api/storage/notifications"
    - from: "frontend/src/app/(authenticated)/layout.tsx"
      to: "UserNotificationBell"
      via: "import and render in nav area"
      pattern: "UserNotificationBell"
---

<objective>
Complete Phase 28 with admin storage settings, user notification bell for share alerts, and a visual verification checkpoint confirming the full file manager flow works end-to-end.

Purpose: Closes out admin control over storage limits/MIME types, adds per-user share notifications to the nav rail, and validates the complete feature works before handoff to Phase 29.
Output: Admin settings panel, UserNotificationBell component, MinIO running with bucket auto-created.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-storage-service/28-CONTEXT.md
@.planning/phases/28-storage-service/28-03-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns from prior plans. -->

From backend/core/models/user_notification.py (plan 02):
```python
class UserNotification(Base):
    __tablename__ = "user_notifications"
    id: UUID, user_id: UUID, title: str, message: str,
    notification_type: str,  # "file_shared"
    is_read: bool, metadata_json: dict | None, created_at: datetime
```

From backend/core/models/system_config.py (existing):
```python
class SystemConfig(Base):
    __tablename__ = "system_config"
    # key/value store for admin-configurable settings
    # Pattern: GET /api/admin/system-config, PUT /api/admin/system-config/{key}
```

Existing admin storage settings keys to add:
- `"storage.max_file_size_mb"` → integer (default 100)
- `"storage.allowed_mime_types"` → JSON array of MIME type strings

From frontend/src/components/admin/ (existing notification-bell.tsx pattern):
- Admin notification bell polls /api/admin/notifications
- UserNotificationBell should mirror this pattern but poll /api/storage/notifications
- Keep them separate (per Phase 26 decision: admin and user notifications are separate)

Nav rail (`frontend/src/components/nav-rail.tsx`):
- UserNotificationBell goes in the nav rail bottom area (near user avatar/profile)
- Different from admin bell (which is admin-panel only)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Admin storage settings API + admin panel settings page + user notification bell</name>
  <files>backend/api/routes/admin_storage.py, backend/api/routes/storage.py, backend/main.py, frontend/src/app/(authenticated)/admin/storage/page.tsx, frontend/src/components/user-notification-bell.tsx, frontend/src/app/(authenticated)/layout.tsx</files>
  <action>
1. Create `backend/api/routes/admin_storage.py`:
   - `router = APIRouter(prefix="/api/admin/storage", tags=["admin-storage"])`
   - All endpoints require `it-admin` role (use existing RBAC dependency pattern from other admin routes).
   - `GET /api/admin/storage/settings` — reads `system_config` keys `storage.max_file_size_mb` and `storage.allowed_mime_types`. Returns `{"max_file_size_mb": 100, "allowed_mime_types": [...]}`. If keys not present, returns defaults.
   - `PUT /api/admin/storage/settings` — body: `{"max_file_size_mb": int, "allowed_mime_types": list[str]}`. Upserts both `system_config` keys. Returns updated settings.

   Add notification endpoints to the storage router (edit `backend/api/routes/storage.py` from plan 02):
   - `GET /api/storage/notifications` — returns `UserNotification[]` for `current_user.id` where `is_read=False`, ordered by `created_at DESC`, limit 20
   - `POST /api/storage/notifications/{notification_id}/read` — marks notification as read (`is_read=True`)
   - `POST /api/storage/notifications/read-all` — marks all notifications for user as read

   Register `admin_storage_router` in `backend/main.py`:
   - `from api.routes.admin_storage import router as admin_storage_router`
   - `app.include_router(admin_storage_router)`

2. Create `frontend/src/app/(authenticated)/admin/storage/page.tsx`:
   - `"use client"` directive.
   - Fetches `GET /api/admin/storage/settings` on mount.
   - Form with two fields:
     - "Max file size (MB)": number input, min=1, max=500
     - "Allowed file types": comma-separated MIME types in a textarea (or editable tag list)
   - "Save" button: PUT `/api/admin/storage/settings`; show success/error toast.
   - Add link to this page in existing admin navigation (wherever storage settings would logically go — check existing admin nav pattern; add "Storage" section under admin settings).

3. Create `frontend/src/components/user-notification-bell.tsx`:
   - `"use client"` directive.
   - Polls `GET /api/storage/notifications` on mount and every 30s.
   - Shows `Bell` icon (lucide-react) with a red badge number when `unread_count > 0`.
   - On click: opens a dropdown showing last 5 unread notifications with title + timestamp.
   - Clicking a notification: marks as read (POST `/api/storage/notifications/{id}/read`), badge decrements.
   - "Mark all as read" button at bottom of dropdown.
   - Component is `export default function UserNotificationBell()`.
   - Separation: this is NOT the admin notification bell. It only shows `user_notifications` (file shares, etc.), not admin notifications.

4. Update `frontend/src/app/(authenticated)/layout.tsx`:
   - Import `UserNotificationBell`.
   - Render it in the nav rail area (bottom section near user profile/avatar), visible to all authenticated users.
   - Check current layout structure — place it consistently with existing nav rail bottom items.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build 2>&1 | tail -10</automated>
  </verify>
  <done>`pnpm run build` passes; UserNotificationBell renders in layout; admin storage settings page accessible at /admin/storage; notification endpoints added to storage router; admin_storage_router registered in main.py</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full file manager flow end-to-end verification</name>
  <files>N/A</files>
  <action>Human verifies the complete Phase 28 Storage Service end-to-end. All code was automated in Tasks 1-8 across plans 01-04.</action>
  <verify>
Prerequisites: Add to root .env — MINIO_ROOT_USER=minio_admin, MINIO_ROOT_PASSWORD=miniopassword123, and to backend/.env — MINIO_ACCESS_KEY=minio_admin, MINIO_SECRET_KEY=miniopassword123. Then run `just up`.

1. MinIO health: `curl http://localhost:9000/minio/health/live` — returns 200
2. MinIO console: visit http://localhost:9001, log in with minio_admin / miniopassword123 — verify `blitz-files` bucket exists
3. Navigate to http://localhost:3000/files — verify Files page loads with sidebar + main area (no JS errors in console)
4. Nav rail: verify "Files" tab appears between Skills and bottom items
5. Upload a test PDF file (drag-and-drop or Upload button) — verify file appears in grid view with name + icon
6. Upload same PDF again — verify dedup dialog appears with 3 choices (Keep both / Replace / Skip)
7. Switch to list view — verify name/size/date/owner columns visible
8. Create a new folder — verify it appears in sidebar folder tree
9. Click into folder — verify breadcrumb updates (My Files > New Folder)
10. Click "..." on a file, select "Share" — verify share dialog opens; type partial email — verify typeahead results appear
11. Complete a share — verify notification bell badge increments (or check DB: `docker exec -it blitz-postgres psql -U blitz blitz -c "SELECT * FROM user_notifications LIMIT 5;"`)
12. Click "..." on a PDF file, click "Add to Memory" — verify toast appears; brain badge visible on file icon
13. Click "..." on a PNG file — verify "Add to Memory" is greyed out with tooltip
14. Visit http://localhost:3000/admin/storage — verify max file size and allowed MIME types fields present
  </verify>
  <done>Human confirms all 14 verification steps pass. Type "approved" to continue, or describe issues.</done>
</task>

</tasks>

<verification>
1. `cd /home/tungmv/Projects/hox-agentos/frontend && pnpm run build` — exits 0
2. `cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q` — 946+ tests pass, no regressions
3. `curl http://localhost:9000/minio/health/live` — 200 (after `just up`)
4. `docker exec -it blitz-postgres psql -U blitz blitz -c "\dt storage_*"` — shows 3 tables
5. `grep "include_router.*admin_storage\|include_router.*storage" /home/tungmv/Projects/hox-agentos/backend/main.py` — both routers registered
</verification>

<success_criteria>
- MinIO health check passes; blitz-files bucket auto-created by minio-init container
- Admin can configure storage limits from /admin/storage panel
- UserNotificationBell shows share notifications; badge clears on mark-as-read
- Full file upload, share, and memory indexing flow works end-to-end (verified by human checkpoint)
- pnpm run build passes; pytest suite has no regressions
- All 6 STOR requirements addressed across plans 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/28-storage-service/28-04-SUMMARY.md`
</output>
