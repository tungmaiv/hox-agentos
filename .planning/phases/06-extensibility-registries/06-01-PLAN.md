---
phase: 06-extensibility-registries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models/agent_definition.py
  - backend/core/models/tool_definition.py
  - backend/core/models/skill_definition.py
  - backend/core/models/artifact_permission.py
  - backend/core/models/role_permission.py
  - backend/core/models/__init__.py
  - backend/core/models/mcp_server.py
  - backend/core/schemas/registry.py
  - backend/alembic/versions/014_extensibility_registries.py
  - backend/tests/test_registry_models.py
autonomous: true
requirements:
  - EXTD-01

must_haves:
  truths:
    - "Every artifact type (agent, tool, skill, MCP server) has a dedicated DB table with name, description, version, status, and type-specific columns"
    - "artifact_permissions table supports per-artifact per-role access control with default-allow semantics"
    - "role_permissions table replaces hardcoded ROLE_PERMISSIONS dict with identical seed data"
    - "Pydantic v2 schemas exist for all CRUD request/response types"
    - "Existing mcp_servers table is evolved with version, display_name, and status columns"
  artifacts:
    - path: "backend/core/models/agent_definition.py"
      provides: "AgentDefinition ORM model"
      contains: "class AgentDefinition"
    - path: "backend/core/models/tool_definition.py"
      provides: "ToolDefinition ORM model"
      contains: "class ToolDefinition"
    - path: "backend/core/models/skill_definition.py"
      provides: "SkillDefinition ORM model with instructional and procedural types"
      contains: "class SkillDefinition"
    - path: "backend/core/models/artifact_permission.py"
      provides: "ArtifactPermission ORM model"
      contains: "class ArtifactPermission"
    - path: "backend/core/models/role_permission.py"
      provides: "RolePermission ORM model"
      contains: "class RolePermission"
    - path: "backend/core/schemas/registry.py"
      provides: "Pydantic request/response schemas for all registry CRUD"
      min_lines: 100
    - path: "backend/alembic/versions/014_extensibility_registries.py"
      provides: "Alembic migration creating tables and seeding data"
      contains: "def upgrade"
  key_links:
    - from: "backend/core/models/__init__.py"
      to: "all new model files"
      via: "import re-exports"
      pattern: "from core\\.models\\.(agent_definition|tool_definition|skill_definition|artifact_permission|role_permission) import"
    - from: "backend/alembic/versions/014_extensibility_registries.py"
      to: "all new tables"
      via: "op.create_table + op.bulk_insert seed data"
      pattern: "op\\.create_table"
---

<objective>
Create the database foundation for Phase 6: ORM models for all 5 new tables (agent_definitions, tool_definitions, skill_definitions, artifact_permissions, role_permissions), evolve mcp_servers with new columns, create Alembic migration 014 with seed data, and define all Pydantic v2 request/response schemas for the registry CRUD APIs.

Purpose: Every subsequent plan in Phase 6 depends on these models and schemas. This is the data layer foundation.
Output: 7 model/schema files, 1 migration, 1 test file
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-extensibility-registries/06-CONTEXT.md
@docs/plans/2026-02-28-phase-6-extensibility-registries-design.md (Section 4: Data Model, Section 5: APIs for schema design)
@docs/plans/2026-02-28-phase-6-extensibility-registries-implementation.md (Task 1 + Task 3)
@backend/core/models/__init__.py
@backend/core/models/mcp_server.py
@backend/core/models/tool_acl.py (existing ACL pattern being replaced)
@backend/core/schemas/ (existing schema patterns)
@backend/alembic/versions/013_channel_tables.py (migration pattern reference)
@backend/security/rbac.py (current ROLE_PERMISSIONS dict for seed data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ORM Models + Alembic Migration</name>
  <files>
    backend/core/models/agent_definition.py
    backend/core/models/tool_definition.py
    backend/core/models/skill_definition.py
    backend/core/models/artifact_permission.py
    backend/core/models/role_permission.py
    backend/core/models/__init__.py
    backend/core/models/mcp_server.py
    backend/alembic/versions/014_extensibility_registries.py
    backend/tests/test_registry_models.py
  </files>
  <action>
    Create 5 new ORM model files per the design doc Section 4 schema:

    1. **agent_definition.py**: AgentDefinition with id (UUID PK), name (TEXT UNIQUE), display_name, description, version, status (String, server_default="active"), handler_module, handler_function, routing_keywords (JSON with JSONB variant), config_json (JSON with JSONB variant), created_at, updated_at. Use `sa.JSON().with_variant(postgresql.JSONB(), "postgresql")` for all JSONB columns (SQLite test compat, same pattern as SystemConfig.value).

    2. **tool_definition.py**: ToolDefinition with id, name (UNIQUE), display_name, description, version, status, handler_type (backend/mcp/sandbox), handler_module (nullable), handler_function (nullable), mcp_server_id (UUID nullable -- no FK to mcp_servers since it may not exist in SQLite tests), mcp_tool_name (nullable), sandbox_required (Boolean, default false), input_schema (JSON/JSONB), output_schema (JSON/JSONB), created_at, updated_at.

    3. **skill_definition.py**: SkillDefinition with all columns from design doc Section 4.2 skill_definitions table. skill_type is String(20) not enum (SQLite compat). slash_command is UNIQUE and nullable. source_type is String(20). security_score, security_report (JSON/JSONB), reviewed_by, reviewed_at, created_by all nullable.

    4. **artifact_permission.py**: ArtifactPermission with id, artifact_type (String), artifact_id (UUID, no FK -- polymorphic), role (String), allowed (Boolean, default true), created_at. UniqueConstraint on (artifact_type, artifact_id, role). Index on (artifact_type, artifact_id) for lookup.

    5. **role_permission.py**: RolePermission with id, role (String), permission (String), created_at. UniqueConstraint on (role, permission). Index on role.

    6. **Evolve mcp_server.py**: Add 3 new columns -- version (Text, nullable), display_name (Text, nullable), status (Text, server_default="active"). Keep existing `is_active` column for backward compat during migration.

    7. **Update __init__.py**: Add imports for all 5 new models.

    8. **Migration 014**: Create `backend/alembic/versions/014_extensibility_registries.py` with down_revision='013'. Creates all 5 tables, adds 3 columns to mcp_servers. Seeds role_permissions with exact values from current ROLE_PERMISSIONS dict in security/rbac.py (5 roles: employee, manager, team-lead, it-admin, executive). Seeds agent_definitions with 4 rows (email_agent, calendar_agent, project_agent, master_agent -- get handler_module/handler_function from current master_agent.py). Does NOT seed tool_definitions from _registry dict -- that happens at runtime in plan 06-04. Use `op.bulk_insert()` for seeds.

    9. **Tests**: Create test_registry_models.py with SQLite in-memory fixture. Test CRUD for each model type, test unique constraint enforcement, test nullable/default columns. Follow the event-loop fixture pattern from existing tests (asyncio.new_event_loop + loop.run_until_complete).
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/test_registry_models.py -v</automated>
    <manual>Verify 7+ tests pass covering all 5 model types plus uniqueness constraint</manual>
  </verify>
  <done>All 5 new models importable, mcp_server.py has new columns, migration 014 exists, all model tests pass, full test suite has no regressions</done>
</task>

<task type="auto">
  <name>Task 2: Pydantic Schemas for Registry CRUD</name>
  <files>
    backend/core/schemas/registry.py
  </files>
  <action>
    Create `backend/core/schemas/registry.py` with Pydantic v2 BaseModel schemas:

    **Agent schemas:**
    - AgentDefinitionCreate: name, display_name, description, version, handler_module, handler_function, routing_keywords (list[str] | None), config_json (dict | None)
    - AgentDefinitionUpdate: all fields optional except name
    - AgentDefinitionResponse: all fields including id, status, created_at, updated_at. Use `model_config = ConfigDict(from_attributes=True)`

    **Tool schemas:**
    - ToolDefinitionCreate: name, display_name, description, version, handler_type (Literal["backend","mcp","sandbox"]), handler_module (str | None), handler_function (str | None), mcp_server_id (UUID | None), mcp_tool_name (str | None), sandbox_required (bool = False), input_schema (dict | None), output_schema (dict | None)
    - ToolDefinitionUpdate: all optional
    - ToolDefinitionResponse: all fields with from_attributes

    **Skill schemas:**
    - SkillDefinitionCreate: name, display_name, description, version, skill_type (Literal["instructional","procedural"]), slash_command (str | None), instruction_markdown (str | None), procedure_json (dict | None), input_schema (dict | None), output_schema (dict | None). Add model_validator to enforce: instructional requires instruction_markdown, procedural requires procedure_json
    - SkillDefinitionUpdate: all optional
    - SkillDefinitionResponse: all fields with from_attributes

    **Permission schemas:**
    - ArtifactPermissionSet: artifact_type (Literal["agent","tool","skill","mcp_server"]), roles (list of {role: str, allowed: bool})
    - ArtifactPermissionResponse: id, artifact_type, artifact_id, role, allowed
    - RolePermissionSet: permissions (list[str])
    - RolePermissionResponse: role, permissions (list[str])

    **Shared:**
    - StatusUpdate: status (Literal["active","disabled","deprecated"])
    - SkillImportRequest: source_url (str | None), content (str | None) -- at least one required
    - SkillReviewRequest: decision (Literal["approve","reject"]), notes (str | None)
    - SecurityReportResponse: score (int), factors (dict), recommendation (str)
    - SkillListItem: name, display_name, description, slash_command (for user-facing list)
    - SkillRunResponse: success (bool), output (str), step_outputs (dict | None), failed_step (str | None)
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && python -c "from core.schemas.registry import AgentDefinitionCreate, ToolDefinitionCreate, SkillDefinitionCreate, ArtifactPermissionSet, StatusUpdate, SkillImportRequest; print('All schemas importable')"</automated>
    <manual>Check that SkillDefinitionCreate validator rejects instructional type without instruction_markdown</manual>
  </verify>
  <done>All Pydantic schemas importable, SkillDefinitionCreate cross-field validation works, all response schemas have from_attributes=True</done>
</task>

</tasks>

<verification>
- `cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/test_registry_models.py -v` -- all model tests pass
- `cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/ -q` -- full suite, no regressions
- `python -c "from core.schemas.registry import *"` -- all schemas importable
</verification>

<success_criteria>
- 5 new ORM model files exist and are importable
- mcp_server.py has version, display_name, status columns
- Migration 014 creates all tables and seeds role_permissions + agent_definitions
- Pydantic schemas exist for all CRUD operations with proper validation
- All existing tests still pass (model additions are additive)
</success_criteria>

<output>
After completion, create `.planning/phases/06-extensibility-registries/06-01-SUMMARY.md`
</output>
