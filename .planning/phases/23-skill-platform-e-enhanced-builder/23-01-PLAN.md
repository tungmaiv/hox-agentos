---
phase: 23-skill-platform-e-enhanced-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/026_tool_handler_code.py
  - backend/alembic/versions/027_skill_repo_index.py
  - backend/core/models/skill_repo_index.py
  - backend/core/models/tool_definition.py
  - backend/agents/state/artifact_builder_types.py
  - tests/skills/test_builder_generate.py
  - tests/skills/test_similar_skills.py
  - tests/skills/test_security_gate.py
autonomous: true
requirements:
  - SKBLD-01
  - SKBLD-02
  - SKBLD-03
  - SKBLD-04
  - SKBLD-05
  - SKBLD-06
  - SKBLD-07
  - SKBLD-08

must_haves:
  truths:
    - "Migration 026 adds handler_code TEXT (nullable) to tool_definitions"
    - "Migration 027 creates skill_repo_index table with vector(1024) embedding column and HNSW index"
    - "SkillRepoIndex ORM model is importable and maps to the skill_repo_index table"
    - "ArtifactBuilderState TypedDict has four new fields: similar_skills, security_report, fork_source, handler_code"
    - "Test stubs for builder_generate, similar_skills, and security_gate exist and are importable without error"
  artifacts:
    - path: "backend/alembic/versions/026_tool_handler_code.py"
      provides: "handler_code column migration"
      contains: "handler_code"
    - path: "backend/alembic/versions/027_skill_repo_index.py"
      provides: "skill_repo_index table + HNSW vector index"
      contains: "skill_repo_index"
    - path: "backend/core/models/skill_repo_index.py"
      provides: "SkillRepoIndex SQLAlchemy ORM model"
      exports: ["SkillRepoIndex"]
    - path: "backend/agents/state/artifact_builder_types.py"
      provides: "Extended ArtifactBuilderState TypedDict"
      contains: "similar_skills"
    - path: "tests/skills/test_builder_generate.py"
      provides: "Test stubs for SKBLD-01, 02, 03"
    - path: "tests/skills/test_similar_skills.py"
      provides: "Test stubs for SKBLD-04, 05"
    - path: "tests/skills/test_security_gate.py"
      provides: "Test stubs for SKBLD-06, 08"
  key_links:
    - from: "backend/alembic/versions/027_skill_repo_index.py"
      to: "backend/core/models/skill_repo_index.py"
      via: "table name 'skill_repo_index' must match ORM __tablename__"
      pattern: "skill_repo_index"
    - from: "backend/alembic/versions/026_tool_handler_code.py"
      to: "backend/core/models/tool_definition.py"
      via: "column added in migration must be reflected in ORM model"
      pattern: "handler_code"
---

<objective>
Lay the DB and type foundation for Phase 23. Two Alembic migrations, a new ORM model, TypedDict extension, and Wave 0 test stubs — all downstream plans (02, 03, 04) depend on these artifacts.

Purpose: Plans 02-04 run in parallel in Wave 2 and all need the extended ArtifactBuilderState, the skill_repo_index table, and pre-existing test file stubs to write tests into.
Output: Migration files 026 and 027, SkillRepoIndex ORM, extended ArtifactBuilderState, three test stub files.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-skill-platform-e-enhanced-builder/23-CONTEXT.md
@.planning/phases/23-skill-platform-e-enhanced-builder/23-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/agents/state/artifact_builder_types.py (current state — EXTEND this file):
```python
class ArtifactBuilderState(TypedDict):
    messages: Annotated[list[BaseMessage], add_messages]
    artifact_type: str | None
    artifact_draft: dict | None
    validation_errors: list[str]
    is_complete: bool
    form_name: str | None
    form_description: str | None
    form_version: str | None
    form_required_permissions: list[str] | None
    form_model_alias: str | None
    form_system_prompt: str | None
    form_handler_module: str | None
    form_sandbox_required: bool | None
    form_entry_point: str | None
    form_url: str | None
    clone_source_name: str | None
```

From backend/core/models/tool_definition.py — add handler_code TEXT column (nullable, no variant needed — plain TEXT not JSONB).

From backend/alembic/versions/ — migration chain: last sequential is 025 (skill_is_promoted). Next is 026. Migration 027 follows 026. Do NOT create branching heads.

From backend/memory/long_term.py — pgvector cosine pattern to replicate in SkillRepoIndex:
```python
# ORM model column:
embedding = Column(Vector(1024), nullable=True)
# Query pattern:
.order_by(SkillRepoIndex.embedding.cosine_distance(query_embedding))
```

From backend/core/models/memory_fact.py (pattern for ORM model with vector column):
```python
from pgvector.sqlalchemy import Vector
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations 026 + 027 and SkillRepoIndex ORM model</name>
  <files>
    backend/alembic/versions/026_tool_handler_code.py
    backend/alembic/versions/027_skill_repo_index.py
    backend/core/models/skill_repo_index.py
    backend/core/models/tool_definition.py
  </files>
  <behavior>
    - Migration 026 adds `handler_code TEXT NULL` to `tool_definitions` table; down_revision points to the 025 migration revision ID (check backend/alembic/versions/ for exact ID)
    - Migration 027 creates `skill_repo_index` table with columns: id UUID PK, repository_id UUID NOT NULL, skill_name TEXT NOT NULL, description TEXT NULL, source_url TEXT NULL, category TEXT NULL, tags JSON NULL, embedding vector(1024) NULL, synced_at TIMESTAMPTZ server_default=now(); creates HNSW index using postgresql_using="hnsw" with m=16, ef_construction=64, postgresql_ops={"embedding": "vector_cosine_ops"}; down_revision = 026's revision ID
    - SkillRepoIndex ORM model has __tablename__ = "skill_repo_index", all columns matching migration, embedding column uses Vector(1024) from pgvector.sqlalchemy
    - tool_definition.py gets handler_code = Column(Text, nullable=True) added to the ORM model
  </behavior>
  <action>
    1. Read the last migration file in backend/alembic/versions/ to get the current head revision ID.
    2. Create 026_tool_handler_code.py: down_revision = {current_head}, revision = "026_tool_handler_code" (or a short hex). upgrade() adds handler_code TEXT NULL column to tool_definitions. downgrade() drops it.
    3. Create 027_skill_repo_index.py: down_revision = revision from step 2. upgrade() creates skill_repo_index table + HNSW index. downgrade() drops the table. Use sa.dialects.postgresql.UUID for UUID columns.
    4. Create backend/core/models/skill_repo_index.py: SkillRepoIndex SQLAlchemy model with all columns. Import Vector from pgvector.sqlalchemy. Export SkillRepoIndex class.
    5. In backend/core/models/tool_definition.py: add `handler_code = Column(Text, nullable=True)` alongside existing columns. Do NOT change any other columns.
    6. Do NOT run `just migrate` — migrations will be applied in docker exec during verification.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/python -c "from core.models.skill_repo_index import SkillRepoIndex; from agents.state.artifact_builder_types import ArtifactBuilderState; print('imports ok')"</automated>
  </verify>
  <done>Both migration files have valid Python syntax; SkillRepoIndex is importable; tool_definition.py has handler_code column. No alembic branching heads (026 → 027 linear chain).</done>
</task>

<task type="auto">
  <name>Task 2: Extend ArtifactBuilderState and create Wave 0 test stubs</name>
  <files>
    backend/agents/state/artifact_builder_types.py
    tests/skills/test_builder_generate.py
    tests/skills/test_similar_skills.py
    tests/skills/test_security_gate.py
  </files>
  <action>
    1. In artifact_builder_types.py, add four new fields at the end of ArtifactBuilderState (preserve all existing fields unchanged):
       - `similar_skills: list[dict] | None` — top-k results from repo search; each dict has keys: name, description, repository_name, source_url, category, tags
       - `security_report: dict | None` — SecurityReport serialized: {score, factors, recommendation, injection_matches}
       - `fork_source: str | None` — attribution string e.g. "skill-name@https://source-url"
       - `handler_code: str | None` — Python stub text for tool artifacts

    2. Create tests/skills/__init__.py if it does not exist (empty file).

    3. Create tests/skills/test_builder_generate.py with stub test functions that import correctly and are marked xfail (failing stubs for Wave 0 — plans 02 will implement):
       - `test_generate_procedural_skill_content` — SKBLD-01
       - `test_generate_instructional_skill_content` — SKBLD-02
       - `test_generate_tool_stub` — SKBLD-03
       Each stub: `@pytest.mark.xfail(reason="Wave 0 stub — implemented in plan 02")` + `assert False`

    4. Create tests/skills/test_similar_skills.py with stubs:
       - `test_search_similar_returns_top_k` — SKBLD-04
       - `test_fork_external_skill` — SKBLD-05
       Each: `@pytest.mark.xfail(reason="Wave 0 stub — implemented in plan 03")` + `assert False`

    5. Create tests/skills/test_security_gate.py with stubs:
       - `test_builder_save_approve` — SKBLD-06
       - `test_builder_save_pending_review` — SKBLD-06
       - `test_builder_inline_approve` — SKBLD-08
       Each: `@pytest.mark.xfail(reason="Wave 0 stub — implemented in plan 04")` + `assert False`
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/skills/test_builder_generate.py tests/skills/test_similar_skills.py tests/skills/test_security_gate.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>ArtifactBuilderState has four new fields. All three test stub files exist and pytest collects them without import errors (xfail tests show as 'x' in output, not 'E').</done>
</task>

</tasks>

<verification>
1. `PYTHONPATH=. .venv/bin/python -c "from core.models.skill_repo_index import SkillRepoIndex; print(SkillRepoIndex.__tablename__)"` prints `skill_repo_index`
2. `PYTHONPATH=. .venv/bin/python -c "from agents.state.artifact_builder_types import ArtifactBuilderState; assert 'similar_skills' in ArtifactBuilderState.__annotations__; print('ok')"` passes
3. `PYTHONPATH=. .venv/bin/pytest tests/skills/ -q` — all 8 stubs collected, 0 errors (xfail not errors)
4. `PYTHONPATH=. .venv/bin/pytest tests/ -q` — full suite remains green (no regressions)
</verification>

<success_criteria>
- Migration 026: adds handler_code TEXT NULL to tool_definitions
- Migration 027: creates skill_repo_index table with vector(1024) column and HNSW cosine index
- SkillRepoIndex ORM importable and consistent with migration DDL
- ArtifactBuilderState has similar_skills, security_report, fork_source, handler_code fields
- 8 xfail test stubs collected without import errors
- Full test suite (719+) still green
</success_criteria>

<output>
After completion, create `.planning/phases/23-skill-platform-e-enhanced-builder/23-01-SUMMARY.md`
</output>
