---
phase: 23-skill-platform-e-enhanced-builder
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - backend/agents/artifact_builder.py
  - backend/agents/artifact_builder_prompts.py
  - backend/skills/importer.py
  - backend/api/routes/admin_skills.py
  - backend/api/routes/admin_tools.py
  - tests/skills/test_builder_generate.py
  - tests/test_skill_importer.py
autonomous: true
requirements:
  - SKBLD-01
  - SKBLD-02
  - SKBLD-03

must_haves:
  truths:
    - "Builder generates complete procedure_json for procedural skill descriptions in one LLM shot"
    - "Builder generates instruction_markdown for instructional skill descriptions in one LLM shot"
    - "Builder generates Python stub with InputModel/OutputModel Pydantic classes for tool artifact descriptions"
    - "Generated tool stub is auto-registered as a pending_stub tool in the DB via POST /api/admin/tools"
    - "Claude Code GitHub URLs and raw YAML are converted to agentskills.io fields via import_from_claude_code_yaml()"
    - "GitHub file URLs (github.com/*/blob/*) are converted to raw.githubusercontent.com URLs before fetching"
  artifacts:
    - path: "backend/agents/artifact_builder.py"
      provides: "generate_skill_content_node LangGraph node"
      contains: "generate_skill_content"
    - path: "backend/agents/artifact_builder_prompts.py"
      provides: "Prompts for full skill content generation"
      contains: "get_skill_generation_prompt"
    - path: "backend/skills/importer.py"
      provides: "import_from_claude_code_yaml() method"
      exports: ["import_from_claude_code_yaml"]
    - path: "backend/api/routes/admin_tools.py"
      provides: "PATCH /{tool_id}/activate-stub endpoint"
      contains: "activate-stub"
  key_links:
    - from: "backend/agents/artifact_builder.py"
      to: "backend/agents/artifact_builder_prompts.py"
      via: "get_skill_generation_prompt() import"
      pattern: "get_skill_generation_prompt"
    - from: "backend/agents/artifact_builder.py"
      to: "backend/agents/state/artifact_builder_types.py"
      via: "handler_code field in ArtifactBuilderState (added in plan 01)"
      pattern: "handler_code"
    - from: "backend/skills/importer.py"
      to: "backend/gateway/tool_registry.py"
      via: "known tool name lookup for Claude Code tool reference mapping"
      pattern: "tool_registry"
---

<objective>
Add full skill content generation to the builder agent (SKBLD-01, 02, 03) and the Claude Code import adapter (support for Claude Code GitHub URLs + raw YAML paste).

Purpose: Transforms the builder from a metadata-only assistant into one that generates complete, executable skill definitions in a single LLM response. Also allows Claude Code skills to be imported as draft starting points.
Output: generate_skill_content_node in builder, prompts, handler_code stub flow, activate-stub endpoint, Claude Code import adapter.
</objective>

<execution_context>
@/home/tungmv/.claude/get-shit-done/workflows/execute-plan.md
@/home/tungmv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-skill-platform-e-enhanced-builder/23-CONTEXT.md
@.planning/phases/23-skill-platform-e-enhanced-builder/23-RESEARCH.md
@.planning/phases/23-skill-platform-e-enhanced-builder/23-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts extracted from codebase. No exploration needed. -->

From backend/agents/artifact_builder.py — existing graph topology:
```
START → route_intent (conditional)
  |-> gather_type            (if artifact_type not set)
  |-> gather_details         (if type set but draft incomplete)
  |-> validate_and_present   (if draft ready)
  |-> fill_form_node         (after fill_form tool call)
Each node → END
```

Existing copilotkit_emit_state pattern (in _emit_builder_state):
```python
await copilotkit_emit_state(config, {
    "artifact_type": ...,
    "artifact_draft": ...,
    "validation_errors": ...,
    "is_complete": ...,
})
```
Phase 23 adds: similar_skills, security_report, fork_source, handler_code to these emit calls.

From backend/agents/state/artifact_builder_types.py (after plan 01):
```python
class ArtifactBuilderState(TypedDict):
    # ... existing fields ...
    similar_skills: list[dict] | None
    security_report: dict | None
    fork_source: str | None
    handler_code: str | None
```

From backend/skills/importer.py (existing methods):
```python
class SkillImporter:
    def parse_skill_md(self, content: str) -> dict[str, Any]: ...
    def import_from_zip(self, zip_bytes: bytes) -> dict[str, Any]: ...
    async def import_from_url(self, url: str) -> dict[str, Any]: ...
```

From backend/core/models/tool_definition.py (after plan 01):
```python
class ToolDefinition(Base):
    status: str  # String(20) — accepts "pending_stub" without migration
    handler_code: Text  # nullable — added in migration 026
```

From backend/api/routes/admin_tools.py — pattern for existing endpoints:
- `_require_registry_manager` dependency for auth gate
- `get_session` dependency for DB

GitHub URL conversion (anti-pitfall from RESEARCH.md):
```python
# github.com/user/repo/blob/main/skill.yaml
# → raw.githubusercontent.com/user/repo/main/skill.yaml
import re
url = re.sub(
    r"github\.com/([^/]+)/([^/]+)/blob/([^/]+)/(.*)",
    r"raw.githubusercontent.com/\1/\2/\3/\4",
    url
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generate_skill_content_node + prompts</name>
  <files>
    backend/agents/artifact_builder.py
    backend/agents/artifact_builder_prompts.py
    tests/skills/test_builder_generate.py
  </files>
  <behavior>
    - test_generate_procedural_skill_content: mock LLM to return a JSON string with procedure_json containing steps array; call the node; assert artifact_draft['procedure_json'] is set with steps key
    - test_generate_instructional_skill_content: mock LLM to return instruction_markdown text; call the node; assert artifact_draft['instruction_markdown'] is set as a non-empty string
    - test_generate_tool_stub: mock LLM to return Python stub with InputModel and OutputModel; call the node; assert handler_code is set in returned state
  </behavior>
  <action>
    RED: Replace xfail stubs in tests/skills/test_builder_generate.py with real failing tests that import and call generate_skill_content_node. Tests must fail (node does not exist yet).

    GREEN: Implement in artifact_builder_prompts.py:
    - `get_skill_generation_prompt(artifact_type: str, draft: dict) -> str` — system prompt instructing LLM to generate full content. For procedural skills: output JSON with procedure_json.steps (list of dicts with step, tool, prompt fields). For instructional skills: output markdown starting with "# ". For tools: output a Python stub as code block with InputModel/OutputModel Pydantic classes and async handler function.

    In artifact_builder.py:
    - Add `async def _generate_skill_content_node(state: ArtifactBuilderState, config: RunnableConfig) -> dict:` node.
      - Gets artifact_type and artifact_draft from state.
      - Builds prompt via get_skill_generation_prompt().
      - Calls LLM (get_llm("blitz/master")) with the prompt.
      - Parses response: if procedure skill → extract JSON for procedure_json; if instructional → set instruction_markdown; if tool → set handler_code in state.
      - Calls _emit_builder_state() with updated draft + handler_code.
      - Returns updated state dict.
    - In `_route_intent`: add condition — if artifact_type is set AND draft has name+description but no procedure_json/instruction_markdown → route to "generate_skill_content".
    - Register node: `builder.add_node("generate_skill_content", _generate_skill_content_node)`.
    - Add edge: `builder.add_edge("generate_skill_content", END)`.

    Note: "Edit JSON" toggle is frontend-only (Plan 02 does not need to change the backend for that).
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/skills/test_builder_generate.py -q</automated>
  </verify>
  <done>All 3 tests pass: procedural skill gets procedure_json, instructional skill gets instruction_markdown, tool gets handler_code set in state. No xfail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tool stub auto-registration endpoint + Claude Code import adapter</name>
  <files>
    backend/skills/importer.py
    backend/api/routes/admin_tools.py
    tests/test_skill_importer.py
  </files>
  <behavior>
    - test_import_claude_code_yaml: given Claude Code YAML with name/description/tools fields, assert result dict has name, description (from YAML), instruction_markdown seeded from description, and allowed_tools from tools list
    - test_github_raw_url_conversion: given "https://github.com/user/repo/blob/main/skill.yaml", assert output is "https://raw.githubusercontent.com/user/repo/main/skill.yaml"
    - test_activate_stub_endpoint: PATCH /api/admin/tools/{id}/activate-stub with it-admin token changes status from pending_stub to active; handler_code remains set
  </behavior>
  <action>
    RED: In tests/test_skill_importer.py, add the two test functions (test_import_claude_code_yaml and test_github_raw_url_conversion). They should fail because the method does not exist yet. Also add test_activate_stub_endpoint in relevant test file.

    GREEN:

    In backend/skills/importer.py, add method `import_from_claude_code_yaml(self, content: str) -> dict[str, Any]`:
    - Parse YAML using `yaml.safe_load(content)` (PyYAML is already in pyproject.toml).
    - Map fields:
      - `name` → `name` (snake-cased if needed)
      - `description` → `description` AND seed `instruction_markdown` (full description paragraph becomes the body)
      - `tools` (list[str]) → attempt to map each to known tool names; store as `allowed_tools`
      - `trigger` and `when_to_use` → prepend to instruction_markdown as context section "## When to Use\n{when_to_use}\n\n## Trigger\n{trigger}\n\n"
      - `category` → guess from description keywords (claude, email, calendar, etc.) or default to "general"
      - `skill_type` → default "instructional" (Claude Code skills are inherently instructional)
    - Return dict in agentskills.io shape (same keys as parse_skill_md output).

    Add static method `_github_to_raw_url(url: str) -> str` on SkillImporter:
    - Uses regex to convert github.com/*/blob/* URLs to raw.githubusercontent.com.
    - Call this in import_from_url() before fetching when host is github.com.

    In backend/api/routes/admin_tools.py, add:
    `PATCH /api/admin/tools/{tool_id}/activate-stub`
    - Auth: `_require_registry_manager` dependency (same as other admin tool endpoints)
    - Loads ToolDefinition by tool_id; 404 if not found.
    - Validates status == "pending_stub"; 409 if not.
    - Sets status = "active", is_active = True.
    - Commits and returns updated tool dict.
    - Route must be declared BEFORE the /{tool_id} catch-all (use literal path `/activate-stub` before `/{tool_id}` pattern).
    - Log the activation to audit logger: action="tool_stub_activated", tool_id, user_id.
  </action>
  <verify>
    <automated>cd /home/tungmv/Projects/hox-agentos/backend && PYTHONPATH=. .venv/bin/pytest tests/test_skill_importer.py::test_import_claude_code_yaml tests/test_skill_importer.py::test_github_raw_url_conversion -q</automated>
  </verify>
  <done>Claude Code YAML parsed correctly; GitHub URLs converted to raw URLs; activate-stub endpoint changes status to active. Full test suite still green.</done>
</task>

</tasks>

<verification>
1. `PYTHONPATH=. .venv/bin/pytest tests/skills/test_builder_generate.py -q` — 3 tests pass (no xfail)
2. `PYTHONPATH=. .venv/bin/pytest tests/test_skill_importer.py -q` — includes new tests, all green
3. `PYTHONPATH=. .venv/bin/python -c "from agents.artifact_builder import create_artifact_builder_graph; g = create_artifact_builder_graph(); print('generate_skill_content' in g.nodes)"` prints True
4. `PYTHONPATH=. .venv/bin/pytest tests/ -q` — full suite green, count >= 719
</verification>

<success_criteria>
- Builder routes to generate_skill_content_node when name+description are set but content is missing
- Procedural skills get procedure_json with steps; instructional skills get instruction_markdown
- Tool artifacts produce handler_code (Python stub with InputModel/OutputModel)
- import_from_claude_code_yaml() correctly maps name, description, tools, trigger/when_to_use fields
- GitHub blob URLs converted to raw.githubusercontent.com before fetching
- PATCH /api/admin/tools/{id}/activate-stub promotes pending_stub → active
</success_criteria>

<output>
After completion, create `.planning/phases/23-skill-platform-e-enhanced-builder/23-02-SUMMARY.md`
</output>
